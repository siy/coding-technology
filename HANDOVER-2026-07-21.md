# Handover — 2026-07-21 (session: pfd-editor — release train shipped, courses built, reflection article)

Huge session. Started from the staged release train (HANDOVER-2026-07-17) and ran it all
the way out, then built the two method courses, fixed a live rendering bug, and wrote the
follow-up article. Everything below is SHIPPED and verified live unless marked otherwise.

## TL;DR — everything is out

- **AS manuscript 0.3.12 → 1.0.0**, released. **The whole release train fired** (PR #35):
  PFD 2.1.0, JBCT 4.3.0, Architecture Synthesis 1.0.0, site redesign — one consistent
  release. All three books LIVE on Leanpub, site deployed to pragmatica.dev.
- **Both method courses now live** (PR #38): PFD course (11 lessons) + Architecture
  Synthesis course (13 lessons), plus AS reference-cards and worksheet pages.
- **Follow-up article DRAFTED** (not yet published/committed):
  `articles/less-art-more-engineering-revisited.md`.
- Working tree on `main`, synced. Only untracked files remain (handovers, book-*-meta,
  the new article, etc. — the usual never-commit set + this article).

## Release train (shipped 2026-07-19/20)

Sequence executed from `website-meta/RELEASE-TRAIN.md`:
- **AS pre-ship manuscript work**: 0.3.13 (ch.4→5 "set of claims" repetition dedup),
  0.3.14 (ch.5 budget arithmetic restructured into math→example pairs, Unicode formulas),
  0.3.15 (**external review 2** disposed + applied: ch.4 "What the procedure is,
  mathematically" = constrained-optimization naming + 3 boundaries; ch.3 containment
  defined; recovery-axis asymmetry owned; axes empiricism stated; ledger schema
  articulated; press/inert glossary terms; "only value **in this ledger**" scoping fix).
  Review verbatim + rulings in `book-arch-meta/architecture-synthesis-review.md` +
  `REVIEW-2-DISPOSITION.md`. **Voice "too native for non-native readers?" raised and
  SETTLED: voice stays** (user: "I like it"); if ever acted on, lever = split longest
  periodic paragraphs, never vocabulary.
- **AS 1.0.0 finalized**: CHANGELOG [1.0.0], `build-pdf.sh --final` (86pp draft/28pp sample,
  no "Draft" stamp). Wording gaps fixed pre-ship (ch.7 replication-kit disclosure +
  references now match the LIVE github.com/siy/derivation-artifacts repo).
- **Leanpub**: all three published. Slugs = `jbct-book`, `process-first-design`,
  **`architecture-synthesis-the-next-correct-step`** (the short `architecture-synthesis`
  book was browser writing-mode; API only uploads to upload-mode books — user recreated it
  full-title + upload-mode). PFD sample = the condensed edition (user ruling). Key at
  `../.env-pub` (source it; mask `api_key=` in output). Rate limit 3/min → 80s gaps.
- **AS Leanpub page dressed**: real cover uploaded (rendered from `book-arch-meta/cover.svg`
  → `book-arch-meta/architecture-synthesis-cover.png`, kept); About text (was default) set
  from `scratchpad/architecture-synthesis-about.md`; subtitle "The Next Correct Step".
  Cover/About are browser-UI only (not API).
- **Repo**: PR #35 merged --admin; tags `arch-v1.0.0`/`pfd-v2.1.0`/`jbct-v4.3.0` pushed;
  **book-arch/ JOINED the repo** (manuscript committed; book-arch-meta/ stays private);
  BOOK-VERSIONING.md gained the AS row. PR #36 = AS slug fix on the site links.
- JBCT stamp fix: `book/introduction.md` + `from-process-to-patterns.md` 4.1.1→4.3.0.

## Site fixes (post-release)

- **PR #37** — restored the light/dark **theme toggle** (redesign dropped it from all 5
  templates; CSS data-theme support was intact, only the control was missing → floating
  button + flash-prevention + localStorage) AND corrected Leanpub **author link** to
  `/u/sergiy1yevtushenko`.
- **PR #39** — **dark-mode front-door SVGs**: pipeline diagram + entry-card icons hardcoded
  light-theme ink hex (#1a2840, #4a5468) as SVG attributes → invisible on dark ground.
  Fixed by routing each brand hex through theme tokens, scoped to `.front-door svg`
  (light mode pixel-identical). User confirmed dark mode fine.

## Courses (PR #38) — the big build

PFD (11 lessons) at `/method/pfd/course/`, AS (13) at
`/method/architecture-synthesis/course/`, mirroring JBCT. Plus AS reference-cards page and
(PR #40) AS **worksheet** page at `/method/architecture-synthesis/worksheet/` — makes the
closing lesson's "fill the worksheet" exercise actionable for course-only readers.
- `website/build.js` generalized single-course → **`COURSES` registry** (per-course
  bookDir/layerDir/urlBase/nonLessonSlugs/storagePrefix/nav/footerLinks...); templates
  `lesson.html`/`course-toc.html` parametrized with placeholders. **JBCT kept byte-identical**
  (golden `dist/` snapshot, `diff -r` gated) EXCEPT the intentional bug fix below.
- 24 course-layer files in `website/course/{pfd,architecture-synthesis}/*.md`
  (blurb/learn/note/exercise, apply-to-your-system drills, single-source guardrail — no
  restated prose, no code). AS closing = worksheet + counterexample intake.
- **Latent bug fixed (was live on JBCT)**: `parseCourseLayer`'s `\s*$` terminator matched
  every line-end under /m, truncating every multi-line section after line 1 — the shipped
  JBCT course had been showing only the first learn bullet, cut notes, unparsed exercise
  headers ("Exercise — Exercise") across all 22 lessons. Fixed to `$(?![\s\S])`. Repaired
  JBCT too.
- **Delegation lessons**: lesson authoring parallelizes (2 agents, one per book, wrote to
  separate dirs). The build.js refactor does NOT — an agent stalled mid-stream (API error)
  leaving ZERO persisted work; did it directly, self-verified via golden diff. Course-layer
  files must have NO blank line between a `##`/`###` header and its content under the OLD
  parser; the new parser tolerates both.

## The reflection article (DRAFTED — user's next action: review + publish)

`articles/less-art-more-engineering-revisited.md` — follow-up to "Less Art, More
Engineering" (Medium, Nov 7 2023), building on "Beyond Best Practices" (Medium, Mar 5 2025).
1244 words / ~6 min, plain ASCII (no em-dashes — paste-as-own rule), user's voice.
**Thesis (3-act arc):** 2023 promised measurability → 2025 five criteria (already drifting
"measurable"→"comparable") → 2026 surprise: the cure was **determinism, not measurement**.
Core insight: you can't eliminate subjectivity, only **concentrate** it (into the criteria
and into the business inputs); failure relocates to business decisions. **AI is the earned
climax** — a deterministic procedure is machine-executable; proof = the blind derivations
run by AI operators from the rules alone; completes the 2023 "End of Free Lunch" scaling
argument. Products near-invisible; blind derivation is the single worked proof; one closing
link to pragmatica.dev.
- **Device used:** the semantic drift of "measurable" across the author's own three
  articles (user approved).
- Title shipped in draft: "Less Art, More Engineering: It Wasn't Measurement". Alternatives
  offered: "...I Bet on Measurement" / "The Part of ... I Got Wrong".
- **Not committed, not published.** Cross-post path = article-cross-poster (dev.to/Medium)
  per `reference_publishing`. Publishing = user action (posting public content).

## Open / deferred (all user-call, none blocking)

- **HN announcement POSTPONED** (user: widest reach but highest risk + babysitting). A
  Show HN draft was written this session (title + body + first-comment guidance) — the
  reflection article is the lower-risk on-ramp; if it survives Medium, it de-risks HN.
- **Post-1.0 AS register** (in REVIEW-2-DISPOSITION.md): worked genuine-tie example,
  verify→correct→re-derive convergence loop, ch.3/4 pipeline diagrams.
- **PFD afterword** stays a non-lesson (user agreed). **AS worksheet** now a page (done).
- **Un-audited for dark-mode hardcoded-hex SVGs**: the older article cover images
  (`articles/assets/`?) — likely same root cause as PR #39 if any show dark-on-dark.
- Announce vehicle for the release itself (review copies Poltorak/Yannick, Max Grom
  nine-questions pointer) — still user-side, unchanged from earlier handovers.

## Conventions / state

- Books never committed; never-commit set = book-arch*, book-aether*, HANDOVER-*,
  aether-coder skills, book-*-meta. Commit = explicit ask, single-line conventional, no
  attribution. PRs merged via `gh pr merge --admin` (branch protection), then main + pull.
- Memory updated: `project_arch_book` (SHIPPED), `project_pragmatica_site_vision` (courses
  + parser-bug lesson + delegation lesson), `project_release_workflow` (AS slug, upload-mode
  gotcha, condensed-as-sample, .env-pub key location).
- Golden-snapshot regression pattern (snapshot dist/, diff -r after each change) is the
  reusable safety net for any build.js change — worked perfectly to prove JBCT no-regression.
