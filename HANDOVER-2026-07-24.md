# Handover — 2026-07-24 (session: pfd-editor — decomposition hub named & shipped, two articles, series reconciliation, all three books published)

Very long, productive session. Started from reading HANDOVER-2026-07-21, then: finished the
reflection article, named + shipped the "decomposition hub" across four PFD releases, wrote a
second article, polished the site front door on cofounder feedback, ran a cross-series
numbers/lint reconciliation, and **uploaded + published all three books live to Leanpub myself**.
Everything below is SHIPPED and verified unless marked otherwise.

## TL;DR — everything is out

- **Two articles published** (both dev.to canonical → Medium):
  - *Less Art, More Engineering: It Wasn't Measurement* (the reflection piece; added prior-article
    links + AS link + an origin paragraph).
  - *Software's Second Free Lunch* (new — the GPU-model-of-development-scaling piece). LinkedIn
    announce **drafted, plain ASCII, copied to clipboard — user to post** (not confirmed posted).
- **PFD released 2.2.0 → 2.2.1 → 2.3.0 → 2.3.1**; **AS 1.0.0 → 1.0.1**; **JBCT 4.3.0 → 4.3.1**.
- **Site** front door polished + all book web editions redeployed (Netlify auto-deploy on merge).
- **Leanpub: all three published live 2026-07-24**, verified via `last_published_at`.

## Articles

- **Reflection piece** — `articles/less-art-more-engineering-revisited.md`. Added: links to the two
  prior Medium articles (`.../less-art-more-engineering-c01582feb7cb`,
  `.../beyond-best-practices-e36511c073e2`), "procedure" → `pragmatica.dev/method/architecture-synthesis/`,
  and an **origin paragraph** (organize code → understand low-level structure → PFD → Architecture
  Synthesis). LIVE: dev.to `/siy/less-art-more-engineering-it-wasnt-measurement-2916` · Medium
  `/less-art-more-engineering-it-wasnt-measurement-704a5ebe7c95`.
- **Software's Second Free Lunch** — `articles/softwares-second-free-lunch.md` (retired the earlier
  defensive `brooks-was-right.md` draft). Positive/problem-first arc: development scaling is the
  problem; add-people (Brooks) AND add-AI both fail (AI = workforce, not the parallelization
  strategy — coupling remains); borrow the GPU recipe (uniform + independent units) via driver
  attribution + JBCT structural uniformity; parallelism is fractal (dev ≠ runtime parallelism);
  exposes design-scaling, solved by quasi-linear cohesion; doesn't contradict Brooks (satisfies "no
  communication among them"), governed by Amdahl. Links Herb Sutter's *The Free Lunch Is Over*
  (`gotw.ca/publications/concurrency-ddj.htm`, http — the https cert is broken). LIVE: dev.to
  `/siy/softwares-second-free-lunch-10fk` · Medium `/softwares-second-free-lunch-dbdcfcef0d9a`.
  LinkedIn announce is in `scratchpad/linkedin-post.txt` (both links, plain ASCII) — **user posts it**.

## The decomposition hub — named, exposed, generalized (PFD 2.2.0–2.3.1)

Naming ruling (user): the triple is **change-driver register** (artifact) / **driver attribution**
(mechanism — "attribute each use case to the driver that moves it, group by shared attribution") /
**quasi-linear cohesion** (property; sentence: "driver attribution makes cohesion quasi-linear";
headline contrast quadratic → quasi-linear). All staged as **item 13 in
`book-pfd-meta/PLANNED-CHANGES.md`** (Parts A/B/C).

- **PFD 2.2.0** (`pfd-v2.2.0`, PR #41): Part A — named the mechanism/property in `foundations.md`,
  headlined the payoff in the intro jigsaw section, glossary entries, near-linear→**quasi-linear**;
  **item 11** — mechanical use-case/workflow discriminator + classic-UC (Jacobson/Cockburn) note,
  sharpened glossary `Use case`/`Workflow`.
- **PFD 2.2.1** (`pfd-v2.2.1`, PR #42): the greenfield **honest-limit** paragraph in *Where the
  structure comes from* — scopes change-driver-as-fact to the knowable case (measurable where
  history exists; low-confidence prediction where not); the thin end self-corrects as co-change
  accrues. Answers an interlocutor's "you just relocated the subjective judgment" critique.
- **PFD 2.3.0** (`pfd-v2.3.0`, PR #43): Part B section **"Attribution across contexts"**
  (2×2: project history × org history; four corners incl. inherited-codebase/new-team; error-cost
  inversion; governance-finding vs open-strategic-question output split) + the **numbers-clog pass**
  on the Foundations core vocabulary (de-branded the count-first intros of properties/shapes/
  primitives/moments/parse-levels/telescope; kept argument-bearing counts).
- **PFD 2.3.1** — see series reconciliation below.

## Site front-door polish (cofounder feedback, PR #44)

Cofounder reviewed the extracted content (offered a separate visual/mobile pass). Applied on
`website/templates/front-door.html` + `styles/style.css`:
- **Hero subhead** rewritten plain-language (dropped "progressively smaller decision spaces" jargon
  + the free/book line).
- **Counterexamples proof band** added after the pipeline diagram (blind-derivation receipts above
  the fold), styled with site tokens (navy border, amber next-action link, theme-safe).
- **PFD card blurb** fixed — was "nine questions, four shapes, six altitudes" (branded AND wrong:
  four altitudes not six; nine questions is AS's). Now a plain tagline.
- **"Book · from $19"** on all three book CTAs (user gave the $19).
- Footer `pragmaticalabs.io` contradiction **left to the other agent** (fix is at the destination;
  that cleanup is in progress by another agent). User later confirmed site + mobile look fine.

## Series reconciliation — JBCT rc3 rules + AS-mirrored numbers pass (PR #45)

One coordinated PR, three tags, one site deploy. Instructions came from another agent (the rc3 lint
merge) — changes were NOT committed by them; this repo's PR flow (me) made them.
- **JBCT 4.3.1** (`jbct-v4.3.1`): **value-object member order** (JBCT-ORD-01) in
  `book/project-structure.md` — public constants lead, private impl constants at the bottom, method
  relative-order not enforced (reverses "patterns-first"); **test-naming relaxed to ≥2 segments**
  (JBCT-NAM-05) `method_[scenario_]expectation` in `book/chapter-summaries.md:192` **and** the
  `book/systematic-application.md` checklist (my consistency follow-on). Rendered in the JBCT book's
  own spaced-hyphen style, not the spec's em-dashes.
- **AS 1.0.1** (`arch-v1.0.1`): numbers pass — de-branded the label-only sets (driver modes,
  cost-and-risk indicators, path constraints) while **keeping every argument-bearing count** (nine
  questions = output of the membership criterion; six axes = completeness argued, and uniformly
  count-first so de-branding would DIVERGE). Fixed a recovery value-name **drift**:
  `design-the-failure-out` → `design-out` in `book-arch/two-teams.md:52` (only occurrence).
- **PFD 2.3.1** (`pfd-v2.3.1`): brownfield "six indicators / six dimensions" de-branded to match AS.
- **Meta (change 3):** `book-arch-meta/NEXT-STEP-SPEC.md:77` Q4 scope → "per data class/path" — edited
  LOCALLY (book-arch-meta is untracked/never-committed, so NOT in the PR). AS *book* already correct
  (`answer-sheet.md:21`), so no book change for Q4.
- Note: the survey's scary "4 vs 5 failure modes" flag was a **false alarm** (verified: PFD has 5
  general + 4 brownfield-specific; AS's "four" = the four brownfield-specific — consistent).

## Leanpub — all three PUBLISHED LIVE (2026-07-24)

**Correction for the record:** publishing to Leanpub is **scriptable by the assistant** — I wrongly
called it "your manual step" repeatedly until the user nudged. Mechanism (already in the
`release-workflow` memory): `./publish-leanpub.sh <slug> <pdf> [epub] [--sample] [--publish]`, key in
`../.env-pub` (`LEANPUB_API_KEY`, source it, never echo). Did: per book, upload sample (no publish),
then upload full **with `--publish`** (releases both editions), 80s gaps for the 3/min rate limit.
The publish call prints a **harmless "Unexpected Server Error"** (spurious non-JSON body) even on
success — **always verify `last_published_at` moved**:
- JBCT `2026-07-24T08:28:48Z`, PFD `08:32:00Z`, AS `08:35:16Z` — all confirmed live.
- Built artifacts (Leanpub upload sources): `book/jbct-book.pdf`+`.epub` (+`-sample`),
  `book-pfd-meta/process-first-design.pdf` (163pp) + `-condensed.pdf` (sample),
  `book-arch-meta/architecture-synthesis.pdf` (87pp) + `-sample.pdf` (28pp).

## Open / deferred (all user-call, none blocking)

*(Reconciled after the session: LinkedIn announce posted, `pragmaticalabs.io` cleanup done,
HN Show HN dropped, BOOK-VERSIONING.md refreshed. Remaining:)*

- **Older article cover SVGs** — dark-mode hardcoded-hex audit still owed (from HANDOVER-2026-07-21;
  same root cause as the PR #39 front-door fix).
- **AS `check-consistency.sh` findings** — informational style-lint (long-sentence flags in unrelated
  chapters), not errors from this session's edits; not addressed.

## Conventions / state

- Books never committed as PDF/EPUB (gitignored build outputs); `book-arch-meta` / `book-aether*` /
  `HANDOVER-*` / `website-meta` are untracked never-commit; **`book-pfd-meta` IS tracked** (item 13
  staging is committed at `63ea091`). Commit = explicit ask, single-line conventional, no attribution.
- Per-book tags: `jbct-vX.Y.Z` / `pfd-vX.Y.Z` / `arch-vX.Y.Z`. Releases via `gh pr merge --admin`
  (branch protection), then main + pull. **Site auto-deploys via Netlify on merge to main** (~17s);
  `dist/` is gitignored (built on deploy). JBCT book style = spaced hyphens; PFD/AS = em-dashes.
- Memory updated: `project_decomposition_hub` (created; tracks the whole arc through Leanpub-live),
  `reference_arch_decision_master_sheet` (FYI — Subodh KC's 25-layer checklist, a "Chosen"
  counter-example to AS derivation), `MEMORY.md` pointers. `release-workflow` memory already had the
  full Leanpub-publish process (I should have read it sooner).
