# Handover — Aether book (2026-06-22)

For the next session continuing the Aether book. Read **after**
`.claude/scratch/aether-book-editor-brief.md` (original onboarding) and **`BOOK-PLAN.md`**
(the authoritative design + roadmap; this handover is session state and points to it).

## Strategic goal (north star)
One consistent, sustainable Java ecosystem across altitudes: **PFD** (design) → **JBCT**
(code) → **Aether** (ops). The same vocabulary survives every altitude; the Aether book is
where that continuity is *proven* end to end (the loop-closer). Full statement in memory:
`project_ecosystem_vision.md`. Make the continuity *experienced* at the seams (JBCT on-ramp,
forward-refs, Why index), not asserted.

## What the book is
- Premise, audience, full structure, module breakdown: **`BOOK-PLAN.md`** (roadmap).
- Working title placeholder: "Building Applications with Aether" / subtitle "Less art, more
  engineering" (in `build-pdf.sh`). Real title still open.
- Voice: shared `../../oss/content/book-voice.md` + overlay `aether-book-voice.md`.
- Build: `./build-pdf.sh` (draft) / `--final`. Built PDF is gitignored.

## Locked decisions
- Domain = e-commerce **orders** spine, built incrementally. `jbct-loan` / `jbct-realworld`
  are **idiom references only**, not the spine (both loan-domain).
- **Builds on JBCT** (assumes basics; Part 0 is a fast on-ramp, not a re-teach).
- **Problem-driven engine**: problem → analysis → derivation → idiom → integrate-into-spine →
  why. Every rule carries its reason (the Why index appendix).
- Chapter tags: STABLE (write now) / VOLATILE (verify against source; resources are
  behavior-stable per the runtime author) / INVENTED (prototype-gated).
- Two planted through-lines: **knowledge-gathering** (Part 0.1) and
  **assembling-vs-provisioning** (Part 0.3). Echo them.

## Fidelity discipline (do not skip)
- **Source is truth; docs lag.** Always verify against `../pragmatica/aether` (runtime) and
  `../pragmatica/core` (Pragmatica Core / JBCT types) before asserting. The `jbct-coder` agent
  / `jbct` skill are authoritative for Java idioms.
- This caught real doc errors (the catalog's non-existent `db.query()`, a stale lint count).
  Do not trust the skill/catalog blindly.
- Prose rules: no em-dashes; no strawman contrast (state what a thing *is*; only negate a
  belief the reader actually holds) — shared `book-voice.md` §5.

## Progress (drafted)
- **Introduction** ✓
- **Part 0 — On-ramp** ✓ (0.1 knowledge gathering · 0.2 let Java be Java · 0.3 two kinds of DI)
- **Part I — Aether Slice: No Magic** ✓ COMPLETE (1.1 anatomy: written vs generated · 1.2
  lifecycle · 1.3 assembly & provisioning, the two wiring mechanisms · 1.4 config inheritance ·
  1.5 routing)
- **Part II — The Aether model** ✓ (2.1 first slice · 2.2 Ember/Forge/Aether · 2.3 idempotency)
- **Part III — playbook → Module A (Persistence)**: ch1 storing state (`@Sql` + transport
  by priority) · ch2 Aether Store (`@PgSql`) · ch3 schema management & migration · ch4 jOOQ —
  all done. **Module A ch5 + Modules B–F are stubs.**

## Next
1. Module A ch5 — More than one datasource (custom DB qualifiers `@OrderDb`/`@AnalyticsDb`).
2. Module B — Messaging; Module C — Other resources; then pattern modules D/E/F.

## Open / needs Sergiy
- **Saga approach (Module D flagship)** — undecided: durable checkpoint + compensating
  use-cases vs event-log/stream-driven vs a shape the runtime intends. INVENTED idioms need a
  working, tested **prototype** before their code is final.
- Book **title**; **cover** (none yet — build is content-only).
- **Landed tickets (merged 2026-06-24, examples pending)**: **#339** consumes/produces media
  types (Part I 1.5 already says "media type, JSON default"; Module C/routing details it) and
  **#198** API versioning (Module F + routing). Both merged with no example projects yet:
  verify the API against `../pragmatica` source when drafting those chapters; second pass
  against the examples once Sergiy flags them. See `BOOK-PLAN.md` §4b "Landed".

## Cross-session
- Channel: `.claude/scratch/cross-session.md` (gitignored, append-only; handles
  case-sensitive). `pfd-editor` owns the shared `book-voice.md` and the cross-book versioning
  thread.

## State of the tree (nothing committed — all staged for Sergiy's review)
- `book-aether/`, `book-aether-meta/` (new — manuscript, plan, voice overlay, build).
- `.gitignore` (Aether PDF ignore).
- `ai-tools/`: `jbct-coder` agent + `jbct` skill backported & source-anchored;
  `aether-coder` skill minimal drift-fix (incl. lint count → 41).
- `~/.claude/`: `jbct` skill (resolution order: local `../pragmatica` first, then download) +
  `aether-coder` synced. Snapshot at `../oss/config-snapshots/2026-06-20T07-03/`.
- `../oss/content/book-voice.md` (pfd-editor's; their repo).
