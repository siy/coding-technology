# Handover — 2026-08-24 (session: the measurement programme, JBCT 4.8.0 published, PFD 2.6.0)

Continues `HANDOVER-2026-08-17-session-2.md`. **30 commits unpushed on `main`.** All four checkers green.

Versions: JBCT **4.8.0** (tagged, published, readers notified), PFD **2.6.0** (committed, **untagged,
unpublished**), AS 1.1.2, Aether 0.1.0 (draft).

---

## What shipped

| | |
|---|---|
| JBCT 4.8.0 | tagged, live on Leanpub, **readers emailed** |
| PFD 2.6.0 — *The Data Question* | new module, committed, untagged |
| Backlog items 15-19 | data-structures intake, the runs, the tool defect |
| Six measurement runs | all executed and graded |
| `jbct-loan` | five MIXED violations fixed, 734 tests green, **uncommitted** |

**JBCT 4.8.0 is live and readers were notified.** `last_published_at` moved 2026-08-13 → 2026-08-20,
page count 316 → 324. Readers had been on 4.6.0; the release notes therefore covered 4.7.0 as well.
**Caveat:** the notify call returned `{"success":true}` and moved the timestamp a second time, but
`job_status.json` returned `{}` — the job had already drained, so `EmailReadersJob` was never directly
observed. Believed sent; not proven.

**PFD 2.6.0 added *The Data Question*** — 4,495 words between *Architecture Synthesis* and *Edge Cases*.
Its spine is the three-occasions enumeration: the whole record materializes on the read path (no writer,
free), at a spanning invariant (one writer, priced), and at erasure (universal writer, the pressure
case). *Foundations* shed 10,934 → 9,537 words. Full propagation to both appendices and the glossary,
per the five-place rule.

---

## The measurement programme — item 18, all six runs executed

The session's centre of gravity. Every run pre-registered, metric locked before data, misses published.

| Run | Claim | Result |
|---|---|---|
| 1 / 1b | deterministic structure | **see below — three corrections, currently unresolved** |
| 2 | co-change predicts independence | **FAILED** — design defective, claim untested |
| 3 | external corpus base rate | **corpus objection weakened**; found a tool defect |
| 4 | AS design-space completeness | **two missing inputs found** |
| 5 | crossings completeness | lever **weakened**, 62.5% |
| 6 | does composition detection generalize | **yes** — Run 3's confound bounded |

### Run 3 — the corpus objection is answered

48,000 methods across four external business-Java codebases (Fineract, Broadleaf, OpenMRS, Shopizer),
zero parse errors, obtained without a volunteer. JBCT SEQUENCER 8.3-20.4% against external 0.00-0.17%;
residual 5-20% against 23-44%. **The distinction survives a same-domain control** — Fineract vs
jbct-loan (both lending) differ by 63.0, while the two JBCT systems in *different* domains differ by
22.0. That was the designated collapse condition and it held.

### Run 4 — two real gaps in the AS ledger

Both are *missing inputs*: a demand statable on the sheet that no rule prices.

1. **Blast-radius containment presses nothing.** Segment's isolation demand returns "no rule in the
   ledger prices this row". Topology moves on cadence divergence and nothing else.
2. **Recovery time does not reach state storage.** LMAX event-sources to rebuild in-memory state after
   failover; the ledger only presses `event-sourced` from audit obligations.

Also: **the engine derives `single deployable` for Segment in both eras** — the architecture Segment
returned to after two years of 140 services. First time the method has disagreed with a real team in the
direction the team itself later moved.

### Run 6 — the confound is bounded

`shape-census` fires on vavr chains (1.35%), so SEQUENCER measures structure rather than an
`org.pragmatica` import. The corpus search is itself a finding: **public Java business code that
composes with typed error values is rare** — GitHub returns katas and library integrations.

---

## Run 1 — three corrections, and where it actually stands

**This is the item most likely to be misread from a summary, so read the whole section.**

**Run 1** (8 implementers) leaked: two treatment agents read the Pragmatica source and one iterated
against `jbct lint` to zero findings. Clean arm n=2. **P1 not evaluable.** The cause was mine — JBCT
needs an external library whose signatures matter for compilation, idiomatic Java needs nothing outside
the JDK, so only the treatment arm had a reason to leave.

**Run 1b** (10 implementers) fixed isolation by vendoring the signature appendix into the prompt.
**All ten disclosed `READ_OUTSIDE_PROMPT: no`.** Result: treatment shape convergence 77.66 against
control 77.71 — margin **−0.05**. Published as "determinism falsified".

**Then the author dismantled the metric, correctly, in two moves:**

1. *Leaves and routing are determined by the process — how can implementations differ?* Inspection
   confirmed it: **all five treatment implementations have the same skeleton** — parse → fork-join over
   three independent lookups → guards → persist → best-effort confirm.
2. *A Leaf is atomic by definition.* Therefore a 45-vs-25 "LEAF" spread cannot be a difference in
   Leaves. It isn't: `shape-census`'s LEAF bucket means *any method that does not compose*, which
   includes `message()`, `masked()`, `normalized()`, `isWellFormed()`, and failure factories. t1 wrote
   **14 named failure factories**; t4 wrote **five `message()` overrides**. That one stylistic choice
   accounts for most of the gap.

**So P1 measured error-construction style, not structure.** A second defect compounds it: scoring the
use-case file alone gives the control **100.00 convergence with sd 0.0** — because every control
use-case file is 100% UNCLASSIFIED. The control can score perfect agreement *by being uniformly
unclassifiable*. The metric measures agreement in a vocabulary only one arm speaks.

**Current standing: determinism is neither supported nor falsified. The instrument was wrong.**
The "falsified" headline in `CONVERGENCE-RERUN-RESULTS.md` is unsafe and needs a third correction
written into it.

**What survives all of it:** distinctiveness. SEQUENCER 5.0-20.5% against a flat 0.0%, UNCLASSIFIED
0.0-1.7% against 19.2-42.3% — total separation, both runs, and these are *between-arm* differences that
the metric's flaw does not touch.

**Run 1c, agreed with the author:** compare **data dependency graphs**, not method histograms — the
ordered set of external calls, which are joined versus chained, where guards sit, which failure is
absorbed. The book already names the DDG and its three operators. **The ten implementations are already
on disk**; no new agents needed.

---

## The error-construction gap (open, under discussion)

Fell out of the Run 1 forensics and is a genuine book defect.

`error-handling.md:97` — the canonical "Defining Typed Errors" example — **teaches hand-written prose in
`message()`**, in two styles in one code block. `Cause.message()` is abstract in Pragmatica, so every
failure type is *forced* to supply one. Three incompatible styles appeared in the wild: named factories
(t1), `message()` overrides (t4), enum-with-field (`jbct-loan`).

**The author's position: prose in `message()` is an antipattern.** Reasons, sharpened: a rendering
concern in a domain type; duplicates the record's data as prose so the two drift; invites string-matching
in tests, which the 4.7.0 mutation caution explicitly warns against; and redaction must be repeated per
message (three implementations independently masked `TaxId`).

**Proposed fix, not yet ruled on:**

- *Mechanism* — either a `default String message() { return toString(); }` on Pragmatica's `Cause`, or
  a JBCT-side `interface DomainCause extends Cause` carrying that default. Records then render
  themselves, always in sync, with redaction controlled once in the value object's `toString()`.
- *Rule* — record when it carries data, enum constant when it does not; never hand-write prose;
  user-facing text produced at the boundary by an exhaustive switch (which finally *uses* the
  exhaustiveness the book already sells); tests match on type, not string.

**The open question that decides the size of this:** where is the line between a typed failure and a
cheap `Causes.cause("Age 0-150")`? The book uses bare string causes freely for value-object validation.
Proposed line — *whether a caller can act differently on it* — but this needs the author's ruling.

Two smaller ones: should the default compose with `source()` for wrapped causes, and is an enum
constant's derived message just its name?

---

## Tooling

**`shape-census` shipped** in jbct-cli 1.0.0-rc3, unblocking half the programme.

**A tool defect found, reported, fixed, verified.** `MIXED` was unreachable — neither imperative
pattern-mixing nor a monadic chain mixing Sequencer with Fork-Join produced it. Bug report at
`../oss/internal/jbct-cli-mixed-shape-bug.md` with a four-method reproduction including an extraction
control. **Fixed and verified against all five acceptance criteria**, and the implementation is stricter
than my criterion — it fires on different-pattern mixing (PAT-02, LAM-03) and correctly *not* on
same-pattern nesting (NEST-01).

**It immediately found five real violations in `jbct-loan`**, all fixed, 734 tests green:
`LoanAmount.loanAmount` (Fork-Join extracted), `verifyNotExceeding` (ternary → `filter`),
`determineRegularPaymentStatus`, `loanAccountStep`, `creditBureauStep` (inner chains extracted).
**These changes are uncommitted** — `jbct-slice/` is untracked in that repo, so there was no baseline to
diff against and I did not commit into a working state the author may have left deliberately untracked.

---

## Conventions and corrections

- **`../oss/tmp/` is now `../oss/internal/`.** 13 stale references updated across three handovers and
  the PFD backlog. Writing to the old path silently recreates it — that is how a stray `tmp/` appeared.
  Recorded in memory as `reference_oss_internal_directory`.
- **The determinism claim is `book/introduction.md:54`, not `from-process-to-patterns.md:251`.** I cited
  the latter throughout and it is a *presupposition*, not the assertion. Corrected across four files.
- **"Deterministic" carries three senses in the book** — inter-implementer (introduction.md:54, the
  contested one), mechanical refactoring (basic-patterns.md:22, untested and now checkable via MIXED),
  and referential transparency (standard FP usage, unaffected). Only the first was ever at stake.
- **Pre-registration earned its keep repeatedly.** It caught a `--format=C` silent failure, inconsistent
  root sets, an incomplete crossing enumeration, and a misattributed MIXED trio. **Four measurement bugs
  of mine, against two genuine gaps in the methodology.**

## Open

**Ready to act on:**
- **30 commits unpushed.** PFD 2.6.0 untagged and unpublished.
- **Run 1c** — DDG comparison over the ten implementations already on disk.
- **The third correction to `CONVERGENCE-RERUN-RESULTS.md`** — the "falsified" headline is unsafe.
- **The error-construction ruling** (above), then `error-handling.md:97`, a possible Pragmatica default,
  and a lint rule.
- **AS backlog items 1-3** (`book-arch-meta/PLANNED-CHANGES.md`, created this session) — blast-radius
  containment, recovery-time-to-state, mechanical sympathy as a prune rather than an axis.
- **`jbct-loan` fixes uncommitted.**
- **Run 2b** registered (`COCHANGE-VALIDATION-PREDICTIONS-V2.md`) with six defects repaired; needs the
  re-screen under corrected criteria.
- **PFD backlog 15.8 / 15.9** — has-a/is-a into item 9's objections register; expression problem to the
  counterexamples page.

**Carried:** "executable falsification" chapter; seven AS questions audit; corpus sheets six and seven;
article cover SVG dark-mode audit; `/articles/` canonicals on #8 and #11 still 404 on Medium.

**Dirty elsewhere:** `../oss` untracked; two commits unpushed in the private cdm repo.
