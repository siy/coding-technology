# Handover — 2026-06-26

## TL;DR — the cliff edge

**One action is queued and waiting on the user's "go": publish both books to Leanpub.**
Everything is built, committed, and pushed. Nothing has been published or deployed yet.

- **JBCT 4.1.1 → Leanpub `jbct-book`** (currently serves **3.2.1** — this is the big jump to the
  self-contained edition).
- **PFD 1.1.0 → Leanpub `process-first-design`** (currently serves 1.0.0).
- Plan agreed: upload full PDF + EPUB (JBCT) / full PDF + sample (PFD; EPUB deferred), then publish.
  `email_readers` stays **off**. Tool: `./publish-leanpub.sh <slug> <pdf> [epub] [--sample] [--publish]`.
  `LEANPUB_API_KEY` is present in the shell (sources `~/.zshrc`).
- **Leanpub publish is classifier-gated** and outward-facing — it needs explicit user consent at the
  moment of running. The user said "publish both books," then asked to confirm right before firing.

**Site publish is DELIBERATELY HELD until the very end** — after the books are published *and* a site
polish/review pass (the user wants to tweak text/elements and possibly reorder before it goes live).
The site deploys when branch `jbct-4.0-self-contained` is **merged to `main`** (Netlify on push to main).

---

## Git state

- Branch **`jbct-4.0-self-contained`**, pushed to `origin` (`git@github.com:siy/coding-technology.git`).
  HEAD = **`b3ff7c1`**. **Not merged to `main`** (main is untouched; merging it deploys the site).
- Commits on the branch (oldest first):
  - `03c6ba4` make JBCT book self-contained (4.0.0); retire coding guide + series
  - `336889a` sync jbct skill/coder tooling + gitignore (book-aether-meta outputs)
  - `2baa68c` sync jbct skill fundamentals + patterns
  - `b1a59eb` JBCT 4.1.0 — materialization section, altitude trees, version reconciliation
  - `3f5fb55` tools-page toolchain overview; remove Specialized Expertise from services
  - `b3ff7c1` PFD 1.1.0 + JBCT 4.1.1 — change-driver section, per-process-types reframe, PFD heading fix
- **Uncommitted = ONLY the Aether in-flight files** (`ai-tools/skills/aether-coder/*`, `book-aether/`,
  `book-aether-meta/`). These belong to the **Aether-book-editor** session — do NOT commit them.
- PDFs/EPUBs are gitignored build outputs — never staged.

---

## What shipped this session (on the branch)

1. **JBCT made self-contained (4.0.0).** Design on-ramp inlined into the book: process-as-unit + six
   properties + data-follows-process + telescope/cohesion in ch02; recovery triple named in ch06; the
   PFD cross-references converted to optional further-reading. Unique series/guide content salvaged into
   the book. **`CODING_GUIDE.md` and `series/` deleted.** Site reworked to render the JBCT book as the
   free web edition (`website/build.js` + `templates/page.html`, new `book/index.md`); AI tooling
   re-grounded off the deleted guide; all version strings reconciled (home now reads the book version).
2. **JBCT 4.1.0** — ch02 "Materialization: When Structure Earns Code" (logical vs materialized
   workflow; entity = persistence edge and/or cross-field invariant; shared code = exposed coupling);
   ch15 explicit subsystem/system package trees + materialized-workflow placement.
3. **Tools/Services pages** — Tools page (`ai-tools/README.md`, symlinked from `AI-TOOLING.md`) got a
   "JBCT Toolchain" overview (introduces CLI + Maven plugin); Services page (`CONTACT.md`) lost the
   "Specialized Expertise" section.
4. **Reviewer thread — Yannick Loth (IVP author):** applied L1 (per-process-types reframe: not
   duplication, distinct change drivers), L2 (a type is shared iff its change-driver set is
   process-independent), L5 (cohesion-paper DOI → `10.5281/zenodo.20785752`). L3 (telescope = refinement)
   resolved as **composition, not nesting** — no book change. **SRP excluded** from all change-driver
   material — Loth's *Why SRP Is Wrong* refutes it; IVP carries the role.
5. **Decomposition synthesis** — "service boundaries fall where no saga crosses": transaction boundary =
   use case; coordination/saga = workflow altitude within a subsystem; service ≈ subsystem; cut by
   change driver. New PFD Foundations section **"Finding the change driver"** (Parnas/Löwy/IVP
   convergence; ask-forward + measure-backward; org diagnostic). *The Saga is Antipattern* added to PFD
   references. JBCT ch02 reframe aligned (4.1.1).
6. **PFD 1.1.0** built + the section/subsection **font ladder fixed** in `book-pfd-meta/build-pdf.sh`
   (`\section` was colliding with the customized `\subsection` at `\Large`).

---

## Books: ready to publish (verified)

- **JBCT 4.1.1** — `book/jbct-book.pdf` (6.3M), `.epub` (6.7M), + samples. Title page stamps 4.1.1.
- **PFD 1.1.0** — `book-pfd-meta/process-first-design.pdf` (631K, 120pp) + sample. Stamps 1.1.0; font
  ladder fixed (eyeball "Architecture Synthesis" vs "What this module does"). PFD EPUB still deferred
  (the TikZ seat state-machine diagram breaks EPUB).

---

## Site: built, held

- The branch already drops the guide/series and renders the book; nav/footer/sitemap updated.
- **`website/dist/` is slightly stale** — last built before the 4.1.1 ch02 tweak. Re-run
  `cd website && npm run build` before deploy so the web edition picks up the latest ch02.
- A local preview server was running on **`http://localhost:8055`** (`python3 -m http.server 8055
  --directory website/dist`) — likely dead across sessions; restart to preview.
- **To do before deploy:** the polish/review pass (tweak text/elements, possibly reorder), then
  `git checkout main && git merge jbct-4.0-self-contained` (triggers Netlify).

---

## Pending / deferred (not done)

- **Leanpub publish** — queued, awaiting go (see TL;DR). THE next step.
- **Site polish + publish** — last, after books.
- **Article** — `book-pfd-meta/article-service-boundaries-DRAFT.md` ("Service Boundaries Fall Where No
  Saga Crosses", trilogy capstone, plain-ASCII, Löwy spelled correctly). The user is considering
  publishing it (dev.to, continuing his two saga articles). Move to `articles/` when finalizing.
- **Reviewer backlog (undrafted) in `book-pfd-meta/reviewer-feedback.md`:** Poltorak "Integrity at the
  Stateful Edge" + P1–P6, Hetland push-points, and **L4 concept map** (PFD foundations; TikZ/EPUB
  production caveat — start with an ASCII/box map). A future consolidated pass; NOT applied.
- **Yannick exchange ongoing** — he said more comments are coming. Last reply sent was on L3
  (composition-not-nesting; he was mid-rethink of his own IVP nesting model). Fold new replies into the
  "Yannick Loth" round in `reviewer-feedback.md`.
- **Race Condition Theater demo** (from Hetland) — host as a site companion *after* an
  honesty/fidelity/attribution pass (see memory `project_race_condition_theater_demo`).

---

## Conventions / gotchas for the next session

- **Never commit** the Aether in-flight files (`aether-coder/*`, `book-aether/`, `book-aether-meta/`) —
  Aether-book-editor owns them. **Never commit** PDFs/EPUBs (gitignored).
- **Commits:** single line, conventional prefix, no body, no trailers/attribution.
- **Plain ASCII** for any text the user pastes/publishes as his own (LinkedIn, articles) — but keep
  correct **diacritics in names/terms/foreign quotes** (it's "Löwy", not "Lowy"; see memory
  `feedback-avoid-em-dashes-in-sends`).
- **"The document" now means the JBCT book** (`book/`) — `CODING_GUIDE.md` was deleted.
- **Merging the branch to `main` deploys the site** (Netlify on push to main). Don't merge until the
  site polish is done and approved.
- **Per-book versioning** (`BOOK-VERSIONING.md`): JBCT at **4.1.1**, PFD at **1.1.0**.
- Build non-interactively with `</dev/null` (the book build scripts have trailing "open?" prompts).

## Key files

- `book/` (JBCT, 4.1.1) + `book/CHANGELOG.md`; `book-pfd/` (PFD, 1.1.0) + `book-pfd/CHANGELOG.md`
- `book-pfd-meta/reviewer-feedback.md` — the collection log (Loth round, decomposition thread, deferred
  Poltorak/Hetland backlog)
- `book-pfd-meta/finding-change-drivers-DRAFT.md` (also now in PFD foundations),
  `book-pfd-meta/article-service-boundaries-DRAFT.md`
- `website/build.js`, `website/templates/page.html`; `publish-leanpub.sh`
