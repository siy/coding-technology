# Handover — 2026-07-17 (session: fresh-read → convergence → release train; staged 2026-07-15)

**Verified current 2026-07-17:** no repo changes since staging — git status identical to
wrap state, versions unchanged (AS 0.3.11 / PFD 2.1.0 / JBCT 4.3.0), USER-READ-FEEDBACK
still holds only item 1 (applied), RELEASE-TRAIN.md untouched, mailbox quiet since
2026-07-10. The three user gates below remain the only open items.

**Late scope addition 2026-07-17 (user, rode the train):** JBCT's undersold second property
named and landed — **"Hide the machinery, keep the meaning"** (business facts preserved in
types/combinators: return types = fallibility/absence, Option params = domain optionality,
flatMap = dependency, all() = independence, sealed Cause = failure catalog, *State =
lifecycle, missing Result = designed out; "code reads twice", "documentation drifts; types
compile"). Landed: JBCT 4.3.0 capstone section + inventory table in *From Process to
Patterns* + intro positioning/takeaway + both glossaries + course lesson bullet; PFD 2.1.0
shapes/semantic-potential loop-close (non-spine, condensed untouched); site jbct landing
paragraph + front-door JBCT card line. All three rebuilt. Quotables saved to memory.
Also 2026-07-17: **BPMN demoted to the series' convergence register** (same treatment as
IVP/Metapatterns — user ruling after observing experienced readers' allergy to BPMN
mentions): pattern catalog now leads with its DDG lineage; the mapping compressed into one
"convergence worth noting" aside in *Basic Patterns* (corroboration-not-foundation + the
one differentiator: "a process diagram lives beside the system and drifts; here the
process is the code"); BPMN stripped from *Advanced Patterns*' questions table and the
refactoring rule; changelog entry deliberately vague (no acronym — a confession-shaped
entry would spotlight what we're dimming). PFD intro's witness-role mention kept. Same
cleanup swept the **site + AI surface**: jbct-coder/jbct-reviewer agents (root symlinks =
site legacy pages), jbct SKILL.md, README.md — pattern tables lead with DDG lineage, BPMN
columns/equations dropped; root CHANGELOG's historical entries left. NOTE for wrap-up:
ai-tools/agents/jbct-*.md + skills/jbct/SKILL.md + README.md now carry uncommitted changes
from this cleanup (distinct from the pre-existing aether-coder working-tree edits, which
are another session's). Also 0.3.12 (AS): ch. 4 Step 0 cites the null vector by name —
read-feedback item 2, reverses a 0.3.1 repetition-pass ruling.

## TL;DR — where things stand

- **THE RELEASE TRAIN IS FULLY STAGED.** One consistent release — PFD 2.1.0 + JBCT 4.3.0 +
  AS 1.0 + site redesign — prepared end to end; the operative doc is
  `website-meta/RELEASE-TRAIN.md` (state table, gates, AS ship-checklist folded in,
  release-day site swaps with exact strings, day-of order). **Trigger = the user's AS read
  verdict.** Nothing else blocks.
- **AS manuscript at 0.3.11** (was 0.3.6 at session start). Three big passes this session:
  (1) three-persona voice-blind fresh read + skeptic verify (0.3.7, 37 edits, 12 files);
  (2) deployment-topology entry rebuilt on the artifact/unit distinction (0.3.8) and
  extended with the delivery-plumbing/version-matrix pricing (0.3.11) — both user findings
  mid-read; (3) the 11→9 rework (0.3.9/0.3.10).
- **The 11→9 convergence is DONE across the whole estate** (user pulled PFD-side forward):
  AS ch. 2 keeps criterion + count-is-output; PFD 2.1.0 asks the nine natively, changelog
  carries the transition story; JBCT verified clean; site glossary/front-door updated.
  Articles stay historical.
- **JBCT 4.3.0 got an unplanned compile-correctness pass**: the pre-cutover course-fidelity
  audit found the book's worked examples used a non-existent async `recover` (15 sites/
  8 files), `Unit.INSTANCE`, `.orElse(null)`-as-canon, a phantom `Promise.or` (5 sites),
  and a non-compiling two-unwrap Config chain. ALL fixed, every signature verified against
  the 1.0.0-rc1 sources jar. Records: `website-meta/COURSE-FIDELITY-CHECK.md` +
  `BOOK-CODE-FIXES.md`.

## ⚠ Session-start actions

1. **Ask the user for the three open gates** (all recorded in RELEASE-TRAIN.md):
   (a) AS read verdict — the train trigger; (b) create the Leanpub book, slug
   `architecture-synthesis` (unpublished OK — fixes URLs for the release-day site swaps);
   (c) book-arch tracking at ship — join the repo like book-pfd/ or stay private
   (changes wrap-up commit scope). Plus one small verify: JBCT intro's "Based on: JBCT
   v4.1.1" stamp — method-spec version (keep) or stale (one-word fix)?
2. **Mailbox watch was NOT re-armed this session** (07-11 handover action, superseded by
   the day's work). If Aether coordination matters again: poll mtime of
   `~/IdeaProjects/pragmatica/aether/docs/internal/coordination/MAILBOX.md`; last known
   entry 2026-07-10 09:20 (contract amendment).
3. On the user's AS verdict → run the RELEASE-TRAIN.md "Release day" section top to bottom.

## AS book (0.3.6 → 0.3.11) — what changed and why

- **Fresh-read pass (0.3.7):** three isolated voice-blind readers (architect/learner/
  skeptic, spine-order sequential protocol) → `book-arch-meta/FRESH-READ-{ARCHITECT,
  LEARNER,SKEPTIC,SYNTHESIS}.md`; edits per `PROPOSED-EDITS.md` (full before/after);
  skeptic verify: 12 closed/5 improved, its 2 new-problems + 3 unaddressed fixed same day
  (`FRESH-READ-SKEPTIC-VERIFY.md`). Headline fixes: founding move constructive-not-causal
  (rivals enter as inputs-or-debt; A1=C user ruling); Companies House "provably" →
  procedural-vs-informational blindness + parametric-memory limit + miss-as-counter-
  evidence; payroll demoted to weighted import, audit+increment worked in-book on
  Profile 3; axis-order = global cheapest-vector minimization (A4=C; mechanism count
  global); "unforced = debt" got a pre-committed falsifier; fourth scope wall in ch. 12
  ("Acting on the vector", UC exhibit); three-spaces model DEFINED (demand/selection/
  position — user ruling); 99.5% = two nines and a half; RTT canonical 80–150.
- **Deployment entry (0.3.8 + 0.3.11, both user findings):** artifact vs runtime unit as
  the two countables; role-selective form named (decades of record; kill-switch ≠ cadence;
  dormant wake-up shares envelope); network pre-paid on event substrate; unified runtime
  re-grounded ("the platform class is young; the pattern is not" — shrinks Aether
  disclosure exposure); 0.3.11 adds delivery plumbing (pipelines/release/dependency
  streams × artifacts) + the **version matrix** (tested composition ≠ running composition
  under independent cadences) + the single train's growing test gate (contained by build
  tooling, caps out as a cadence demand). No graded derivation flips — checked both times.
- **11→9 (0.3.9):** ch. 2 keeps membership criterion + count-is-output law; audit
  narrative + PFD migration note OUT (→ PFD changelog); ch. 1 promise rewritten (was
  "watch that audit happen" — would have broken the promise ledger); series-note carries
  the cross-book note; folded in: nine-reads-as-nine (bound-mode split into two entries),
  shape-vocab glosses, change-driver "sheet does not require the book" line.
- **Backup:** pre-edit 0.3.6 in scratchpad `book-arch-backup-0.3.6/` (still alive as of
  2026-07-17, but session-temp — PROPOSED-EDITS.md has every before/after as the durable
  revert path).
- Draft 82 pp + sample 26 pp rebuilt at each version.

## PFD 2.1.0 (manuscript ready, UNRELEASED)

Nine questions natively (AS Card-1 names + numbers; four categories preserved; scopes now
four — load per path); per-axis citations renumbered; brownfield walkthroughs renumbered;
spiral-1 scope note; changelog carries the full transition story + "nothing you learned is
wrong" migration note. Ship-gated extras: `series-note.md` (spine + build-pdf.sh CHAPTERS
updated), module→AS-book pointer (decision D, "landing with the AS ship"), recovery-triple
long names (Foundations + glossary). 157 pp/50/24 built; condensed spine intact (367).

## JBCT 4.3.0 (manuscript ready, UNRELEASED)

Phases→stages (migration-strategies, appendix-b, chapter-summaries; site crosswalk
updated); recovery long names; About-the-Series block atop Introduction (safe placement —
no spine/numbering risk; shows in course lesson 1); spine-numbering refactor (was
[Unreleased]); and the **code-correctness pass** — see `BOOK-CODE-FIXES.md` for the
per-site table incl. the retry-aspect redesign on `fold(result -> result.fold(failFn,
Promise::success))` and the idiom taxonomy (fold=async branch, mapError=cause→cause,
orElse=async alternative, sync recover=degrade-to-value). All sweeps green; the one
legitimate `.orElse(null)` is JDK Optional in troubleshooting-faq:247.

## Site (site-redesign branch — 2 commits + working tree, NOT committed)

Freshness pass done (2 stale glossary entries fixed — Use Case scope, Evidence Grades
"provably"; orphans index.md/books.md deleted; crosswalk rows historical for 11→9 and
stages). Course fidelity: layer clean, 2 course-side fixes applied. 41 pages, link check
green. Redirects complete in build.js (incl. CONTACT/MANAGEMENT_PERSPECTIVE → labs).
Release-day swaps (front-door AS card, AS landing Leanpub link) prepared in RELEASE-TRAIN.
RCT demo = fast-follow, NOT in train.

## Working-tree note (uncommitted, from before this session)

`ai-tools/skills/aether-coder/*` (5 files) + `book-pfd-meta/PLANNED-CHANGES.md` were
already modified at session start — not this session's work (except PLANNED-CHANGES item-12
updates, which ARE ours). Wrap-up should batch them consciously.

## Engagement (unchanged this session)

Poltorak: standing send-findings obligation; review copy at release. Yannick: review copy.
Max Grom: nine-questions pointer at announce. Article-2: publication-ready, candidate
announce vehicle. Galyen arc complete.

## Conventions & lessons (this session's additions)

- **Isolated fresh-reader panel works**: 3 personas (target/learner/skeptic), voice-blind,
  spine-order, journal-as-you-read, findings typed+located; reconcile against the voice
  contract in main context; skeptic re-verifies the changed passages. Reusable protocol.
- **Verify API claims against the sources jar before editing book code** — extended the
  `feedback_verify_idioms_against_source` rule; the audit was right every time but the
  verification also caught the fixer's stall-that-wasn't and a fourth defect (Config
  two-unwrap chain).
- **Don't diagnose agent stall too fast**: jbct-fixer "idle" was actually mid-flight
  concurrent work; check artifact state before re-doing. (It caught 2 sites I'd have
  missed: LoadConfig Promise chain, appendix-a cheat-sheet ambiguity.)
- Books never committed; never-commit set unchanged (book-arch*, book-aether*, HANDOVER-*,
  aether-coder skills). Commit = explicit ask, single-line conventional, no attribution.
- Memory updated: `project_arch_book` (train state + all versions), MEMORY.md index.
