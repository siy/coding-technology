# Proposed Edits — AS 0.3.6 → 0.3.7 (fresh-read response)

*Batch of proposed edits addressing the fresh-read synthesis (`FRESH-READ-SYNTHESIS.md`).
**Nothing here is applied to the manuscript.** Each item: location, current text, proposed
text, and the finding it closes. Review / redline per item; I apply the survivors in one
per-file pass, then send the skeptic back at the changed A1/A2 passages to verify.*

**Forks resolved (user, 2026-07-14):** A1 = Option C (constructive thesis, boundary named,
Conway answer surfaced from existing machinery, waterfall quote promoted to exhibit).
A4 = Option C (Step 4 reframed as global vector-minimization; mechanism count is global).

**Guiding constraint throughout:** claim less, precisely — almost every edit is a sentence,
a reframe, or a subtraction in the book's existing disclosure register. Two edits add
material (A2 methods note, A3 in-book worked audit); both land in chapters the readers rated
thin, so they fill rather than bloat. Nothing is added to the front-loaded chapters (2–3)
except the B1 orientation sentence, which *helps* density.

---

# Tier A — structural

## A1a · `two-teams.md` — sharpen founding move (causal → constructive)
**Insert a new paragraph between the current ¶ "…preference with a vocabulary." (:7) and
¶ "There is a better account…" (:9).**

> That "something else" has more candidates than taste. Team structure, the shape of the
> contracts that pay for the work, an appetite for risk — each genuinely bends what a team
> builds, and an account that stops at "not the domain" owes them a place. Each is an input
> with a row on the sheet: ownership boundaries, a delivery seam, a cost ceiling enter as
> answers, priced and scoped like the rest. And where one of them draws a boundary no answer
> forces, the method has a name for what it drew — Chapter 9 calls it an unforced position,
> and prices it as debt.

**Why:** closes the skeptic's founding-move gap (A1) by naming Conway/contract/capital and
folding them in as inputs + the unforced-position category. Sets the thesis as constructive.
Ends on the debt point so the next paragraph ("There is a better account…") keeps its reveal.

## A1b · `axes-and-ledger.md:66` — add org-topology to the "why six axes" test
**Insert after the security-topology rejection (…"they differ along the six, plus their
technology sheet."), before "If a future derivation finds a security demand…".**

> A second candidate earns the same test, because it is the other one every reviewer proposes:
> **team topology** — the org chart, Conway's Law made structural. Run the criterion. Team
> ownership is a real pressure, and Chapter 4 shows where it lands: module boundaries contain
> it at zero deployment cost, and it forces a deployable split only when release cadences
> genuinely diverge — a demand already on the sheet, on its own axis. A team structure that
> splits a system no answer divides is not a seventh axis; it is an unforced position,
> Chapter 9's subject. Two systems with different org charts and identical answers derive the
> same vector; the org chart that overrode it left debt, not a new dimension.

**Why:** answers the skeptic's "the most obvious rival axis is never even raised" at the exact
address he looked, using arguments the book already makes (`derivation.md:33`, ch.9). Parallel
in structure to the security-topology paragraph it follows.

## A1c · `judgment.md` — add the act-on-it boundary as a FOURTH wall (3 edits, one pass)

1. **:33** "The last honesty is about the book itself, and it has three walls." → "…and it has
   **four** walls."
2. **Insert after ¶ "…owner's judgment about the owner's money, which the audit can inform and
   must not usurp." (:39)**, using the section's bolded-lead pattern:

> **Acting on the vector.** The derivation produces the next correct step; whether an
> organization can take it is answered by its contracts and its politics, not by its answer
> sheet. Universal Credit's correct 2013 vector — isolate the paying path from the changing
> rules — was derivable on the day; it was also unbuildable inside a four-vendor waterfall
> procurement that had already drawn the seams. The method delivers the diagnosis at the moment
> it is cheapest to have; the authority to spend it belongs to whoever owns the contracts. A
> method that promised otherwise would be claiming to derive its way out of a negotiation.

3. **:43** rules-exercised tag: "the three walls of scope" → "the four walls of scope".

**Why:** the honest limit A1 concedes (derive ≠ act on it). Ties to UC, sets up A1d. The
count and tag edits keep the chapter self-consistent (the original draft missed both).

## A1d · `brownfield.md:15` — promote the waterfall quote to thesis exhibit
**Append to the paragraph ending "…No answer was ever asked to force it."**

> This is the rival account of architecture — that contracts and org charts draw the seams,
> not demands — caught in the act, and it is exactly what the method is built to name. A
> boundary no answer forces is an unforced position whichever hand drew it; procurement is
> only the most expensive hand. The contract did not disprove the derivation. It stood in for
> one, and the audit trail is the bill.

**Why:** (user-approved) converts the undigested counter-evidence into the book's best exhibit
of the rival account being diagnosed. Consistent with A1a/A1c.

---

## A2 · `derived-blind.md:53` — Companies House methods note (3 fixes)

### A2-i — drop "provably", name the parametric residual, turn the miss into evidence
**Replace** the clause "…the operator provably could not have seen the outcome, and could not
have wanted one." **with:**

> …the operator could not *browse* to the outcome — and, a language model executing a written
> protocol, had no taste to defend and no prediction to protect — while every conclusion it
> drew traces to a cited answer and a named rule. One limit stays on the record: Companies
> House is a well-documented public institution whose engineering writing plausibly sits in
> any modern model's training corpus, so this protocol controlled *contextual* contamination,
> not *parametric* memory. What the run demonstrates is procedural blindness — a derivation
> executable from the sheet and the rulebook alone — not informational blindness, which no
> language-model operator can currently promise. The evidence that the two came apart here is
> the miss itself: the derivation predicted the forced minimum and the real registrar had
> decomposed further; whatever the model had absorbed, it did not quietly copy the documented
> answer. A run steered by memory of the outcome fails *toward* the outcome; this one failed
> away from it.

### A2-ii — separate target-selection from execution-blinding
**Insert immediately after A2-i.**

> A distinction the framing owes the reader: choosing this target was not blind. Companies House
> was picked because a statutory registrar was likely to be decisive about the read/write axis
> either way — a legitimate way to spend a test, and a different act from the execution blinding
> above. The strong claim is about how the derivation *ran*, not about how its subject was
> *chosen*; a read-heavy public registrar is close to a foregone conclusion, and the surprise
> the run earns is the axis moving on cited demand, not the direction it moved.

### A2-iii — pre-commit a disconfirmation (close the unfalsifiable absorption)
**Insert where the topology miss is discussed** (the "rescopes the rule rather than breaking
it" passage).

> To keep that rescoping honest, name what would break the rule rather than bend it. One case
> would: topology fragmentation with no forcing demand *and* no organizational home — no
> separate teams, no divergent cadence, no blast-radius line — structure the unforced-position
> ledger cannot file anywhere. And one trend would, more decisively: if blind runs keep
> under-predicting topology — if the forced minimum sits systematically below what disciplined
> organizations build — then something real is pressing that the sheet does not carry, and
> "unforced" has become this method's word for its own blind spot. Companies House is neither,
> yet; its extra seams have organizational homes. The commitment is that the ledger of misses
> stays open to both.

**Why (all three):** A2 — the strongest-evidence chapter's overclaim, 3-persona convergence.
Every addition sits in the chapter's existing honest-disclosure register; net ~½ page in a
chapter that earns its length. No re-run.

---

## A3 · `derivative.md` — replace the offloaded payroll case with an in-book worked audit

### A3-i — the audit (:11), run on Profile 3 instead of payroll
**Replace** "The worked version ran as one of this method's validations, on the payroll
platform whose full story is told in *Process-First Design*'s brownfield chapter: re-derived
from current answers, the system's long-lived hybrid vector came back *no change* on every
axis — every 'except' and special case re-derived rather than grandfathered — which is what a
healthy system under audit looks like: not fashionable, *cited*." **with:**

> The worked version needs no new system: run it on the enterprise platform Chapter 6 derived.
> Feed `next_step` that vector and its own answer sheet, unchanged, and every position
> re-derives to itself — the read-path split still forced by the 200 ms contract, event-sourced
> pricing still forced by the replay mandate, the distributed store still the one container for
> strict-across-regions. *No change on every axis* is what a healthy system under audit looks
> like: not fashionable, *cited*. (The same audit has also run on a long-lived production
> system — the payroll platform whose full story is *Process-First Design*'s brownfield
> chapter; here the point is the mechanism, and the mechanism is checkable on a system this
> book already built in front of you.)

### A3-ii — the increment (:21), a merge on Profile 3's most expensive axis
**Replace** "The same payroll validation carried the worked case. One input set moved: the
answers that had justified a three-deployable split stopped being true when the integration
that demanded them was retired. The re-derivation moved one axis: merge the deployables back;
every other position re-derived to itself." **with:**

> The same platform carries the increment. One answer moves: the regulation demanding quote
> reconstruction under past rule versions is superseded, and no data class on the sheet now
> carries a replay demand. The pressure matrix implicates exactly the rows that cited replay —
> pricing's storage, and nothing else. Re-derive that axis: event-sourced pricing, the most
> expensive value the whole derivation ever bought, recedes to current-state with an audit
> log. The quote path's separation stays — the 200-millisecond contract and the storm shape
> still force it, and its projections now rebuild from current state — but the versioned event
> schemas, the replay machinery, and the unbounded log fold away. Booking, the distributed
> store, the read path's split — every position whose citations still hold — re-derives
> untouched. One lapsed answer, one axis merged *down*, and the merge is toward the cheaper
> value.

*(The paragraph then continues into the existing "The procedure is direction-neutral… Migration
guides run one way; the derivative runs both." — unchanged; its punchline now lands on an
in-book case.)*

### A3-iii — the tally (`closing.md:5`), by tier instead of a flat "nine" (also closes B5-misses)
**Replace** "Nine derivations against the instruments, four of them blind, fourteen graded
positions on the strictest run, two misses kept and converted into rules." **with:**

> Derivations against the instruments, by kind: three profiles on home ground, four blind
> against systems this book had no hand in, one contradiction the mechanics halted on, one
> national-scale brownfield read against the public record, and an audit run here and at scale
> in the companion volume. Fourteen graded positions on the strictest blind run. One derivation
> miss kept and converted to a rule; and one prediction the method overrode and beat — the
> author's own taste, corrected by his own procedure, a different and better kind of miss to own.

**Why:** A3 — payroll was the only validation never shown; the voice rule makes it a callback,
not evidence. Profile 3 demonstrates both audit outputs in-book (no PFD dependency, no
one-example-one-home breach) and fills ch.9's thin spot (architect's MED energy-dip). A3-iii
also fixes the skeptic's "nine mixes evidentiary kinds" and B5's taste-miss/derivation-miss
conflation in one sentence.
**Apply-time checks:** (1) verify against `derived-blind.md`'s own miss accounting — the
original says "two misses kept and converted into rules"; if the Stack Overflow taste-miss
also seeded a rule, keep that credit ("a lesson kept as its own rule") rather than dropping
it. (2) Confirm the ES-recession walk against Profile 3's full sheet: replay must be the
*only* citation for event-sourced pricing (the separation's citations — 200 ms P99, 99.99%,
storm shape — are unaffected and must be stated as such).

---

## A4 · `derivation.md:23` — reframe Step 4 as global minimization (order-independence)
**Insert a paragraph after Step 4 ("…design it out, and check this option first.").**

> A word on order, because the walk visits axes one after another and a reader is right to ask
> whether a different order lands elsewhere. It does not, and the reason is Chapter 3's own
> definition: selection is *the cheapest vector whose envelope contains the demands* — a
> property of the whole vector, not a sequence of local moves. Which axes move, and toward which
> values, is fixed before resolution begins: pressures are recorded in step 3, from the demands
> against the starting vector, before any axis resolves — so no axis's resolution creates or
> removes another's pressure. What remains is choosing values, and *fewest new mechanisms* is counted over the
> finished vector, not tallied as the walk proceeds — a bus introduced for one axis is free for
> every other that can ride it, whichever order they were written down. The walk is the
> efficient route to a minimum that does not depend on the route; with six axes, a genuinely
> close case is small enough to settle by enumeration.

**And** `root.md:15` — **replace** the derivation.md blurb fragment "…conflict rule + scope
test, ordering principles, the worksheet, the pressure matrix." **with** "…conflict rule +
scope test, why axis order does not matter, the pressure matrix." (leaves "Ordering principles"
to `pathfinding.md`, which owns it).

**Why:** A4 — closes the confluence gap by making the procedure match the book's own global
definition; the global mechanism-count closes the one real order-sensitivity (shared
mechanisms). Fixes the spine mislabel.

---

# Tier B — concrete corrections

## B1 · `axes-and-ledger.md:5` — name the three spaces — **OPEN: needs the author's definition**
The draft below assumes reading (i): **demands → selection → procedure** (from the chapter's
own "Chapter 2 produced demands; Chapter 4 will run the procedure; between them sits the
space where selection actually happens"). A second reading is live: (ii) **provides /
mechanism / cost** — the three coordinates of every ledger entry. Only the author knows which
model the tag and spine promised. Confirm before applying; redraft if (ii) or other.

**Replace** "Chapter 2 produced demands; Chapter 4 will run the procedure; between them sits
the space where selection actually happens — what each position on each axis *provides*,
through which *mechanism*, at what *always-on cost*." **with (if reading (i)):**

> Chapter 2 produced demands; Chapter 4 will run the procedure; between them sits the third and
> least-mapped space — the **selection space**, where a demand meets what each axis position
> *provides*, through which *mechanism*, at what *always-on cost*. Three spaces in a row, then:
> demands, selection, procedure — and the middle one is where architecture is actually decided.

**Why:** B1 — "three-spaces model" was promised by spine + instruments tag, never taught.
This teaches it in one sentence and doubles as an orientation scaffold for the densest chapter.

## B2 · `verification.md:29` — availability-nines label
**Replace** "Three nines and a half — 99.5% — is forty-four hours a year" **with** "Two nines
and a half — 99.5% — is forty-four hours a year". (Hours and the other two rungs are correct.)
**Why:** B2 — verified numeracy slip in the precision chapter.

## B3 · `three-profiles.md:53` — disclose that no blind run selects the unified runtime
**Replace** "(the runtime class Chapter 3 disclosed; the derivation needs the value, and any
instance of it serves)." **with:**

> (the runtime class Chapter 3 disclosed; the derivation needs the *value*, and any instance of
> it serves — this is the one place in the book where that value is selected, in a system the
> author fully controls, and Chapter 7's four blind runs, deriving systems this book had no
> hand in, never demand it).

**Why:** B3 — closes the tension with the book's own Aether disclosure rule; stays within the
≤3-mentions budget (this is the disclosed axis-value mention).

## B4 · `axes-and-ledger.md:63` — reconcile "arithmetic" with "mechanism-counting"
**Append** to "…mechanism-counting is how 'cheapest' stays honest when prices resist
arithmetic." :

> This is the precise sense of Chapter 1's "arithmetic": not that costs add like numbers — they
> do not — but that the comparison is mechanical, counting mechanisms rather than weighing taste.

**Why:** B4 — flags the ch.1→ch.3 precision downgrade as a *refinement*, keeping ch.1's
sanctioned punchline intact.

## B5 · `derived-blind.md:5` — drop the false-symmetry claim
**Replace** "It happens twice below, in both directions." **with** "It happens twice below."
*(The taste-miss vs derivation-miss split is handled in A3-iii's tally sentence; if the
cross-findings section restates "two misses" verbatim, apply the same split there — flagged for
confirmation at apply-time.)*
**Why:** B5 — both realized blind cases resolve the same direction; "in both directions"
overclaims symmetry the chapter does not have.

---

# Tier C — located polish (one pass, after A/B settle)

- **`axes-and-ledger.md:9` — gloss `quorum commit` at first use.** "…what quorum commit cannot
  avoid" → "…what quorum commit cannot avoid (the round-trip floor a write pays to reach a
  quorum of replicas)". *(C: undefined load-bearing term, used 3×.)*
- **`judgment.md:25` — name Allspaw in-body.** "an influential 2010 essay" → "John Allspaw's
  influential 2010 essay". *(C: source used but never named in-body.)*
- **`judgment.md:27` — spell out VOID at first use.** "the VOID incident-data project" → "the
  VOID (Verica Open Incident Database) project". *(C: acronym resolved only in back matter.)*
- **`closing.md:15` — de-jargon the gap-drain disclosure.** "a running gap-drain of deferred
  decisions, verified before building, merged under named hard-stops" → "a running backlog of
  deferred decisions, each verified before building and merged only at named stopping points".
  *(C: private jargon opaque to any outside reader; keeps the disclosure, third of three.)*
- **`three-profiles.md:5` — move the author-graded concession to the opening.** Append to
  "…which is precisely the experimental condition Chapter 1's claim requires." : " — with one
  honesty stated up front rather than saved for the end: these three systems are the book's
  own, and their author grades them; home ground proves the mechanics legible, not the method
  right, which is next chapter's job." *(C: skeptic's "performative honesty" — concession
  currently trails at :63.)*
- **RTT number reconcile — RESOLVED (keep 80–150).** Change `when-derivation-says-no.md:21`
  "70 to 140 milliseconds" → "80 to 150 milliseconds" to match `verification.md`'s canonical
  range. Logic holds: still against the 50 ms promise, still unreachable (80 > 50). Confirm
  `verification.md`'s exact wording at apply-time.

---

# Folded elsewhere / not drafted here

- **Nine-reads-as-eight (`answer-sheet.md`, architect LOW)** — fold into the **11→9 rework**
  already in flight (USER-READ-FEEDBACK item 1); don't touch answer-sheet.md twice.
- **Front-loading signpost** — the B1 sentence is one of the two cheap mitigations; the other
  (an early "the reference cards hold all of this" pointer) is a one-line add I'll propose
  inside the ch.3 pass if you want it. No restructure.
- **Soft-economic contradiction example (skeptic MED)** — deferred post-1.0; adds a whole worked
  case against the density budget, and the physics case already teaches the mechanism.

---

# Apply plan (once reviewed)

## APPLIED 2026-07-14 → manuscript 0.3.7 (all items + 4 late catches)

All batch items applied as specified (with the review fixes). During apply, a completeness
check against the three raw reports caught **four findings that had fallen out of the
synthesis→batch funnel**; applied in the same pass:

1. **Weak-baseline strawman** (skeptic HIGH; missing from synthesis) — `derived-blind.md`
   protocol note: concedes the sharper rival is an architect's registered judgment on the
   same sheets; that comparison exists once (the author's SO prediction, beaten); replication
   kit invites the corpus.
2. **Convergence-anecdote grading** (skeptic MED; in synthesis Tier C, never drafted) —
   `closing.md`: the three arrivals split by evidence kind (contact vs independent).
3. **Rung-zero culture tie-back** (architect LOW; missing from synthesis) —
   `derived-blind.md` SO section: "the engineering culture Chapter 3 left unnamed is this one."
4. **Renegotiation-menu scope discipline** (skeptic MED; in synthesis, dropped from batch) —
   `when-derivation-says-no.md` bend-A2: names why the bend is total-or-nothing (one market,
   no narrower scope — the gate's "survived decomposition intact" collecting its due).

**Deliberately NOT applied (tracked elsewhere):**
- answer-sheet.md items (nine-reads-as-eight; shape-vocab one-clause gloss; change-driver
  "you don't need PFD" reassurance) → fold into the 11→9 rework (USER-READ-FEEDBACK item 1).
- Signposts, author-optional: ch. 3 mid-chapter recap; ch. 7 inter-case recap / Companies
  House ordering; Profile-3 re-grounding clause. (Ch. 3's reference-cards pointer already
  exists at "the full entries live in the reference cards.")
- Soft-economic contradiction worked case → deferred post-1.0.
- pathfinding.md payroll calendar-wall mention → stands as a sanctioned callback (not
  load-bearing validation there).

Backup of pre-edit 0.3.6: scratchpad `book-arch-backup-0.3.6/`. Residual sweep clean
(no "provably"/"in both directions"/"Nine derivations"/"three nines and a half"/"70 to 140"/
"three walls"/"gap-drain"/"ordering principles" outside CHANGELOG). Verification: skeptic
re-read of changed passages → `FRESH-READ-SKEPTIC-VERIFY.md`.

## ROUND 2 — verify-pass results applied (same day)

Skeptic verdicts on 21 original findings: **12 CLOSED, 5 IMPROVED, 3 UNADDRESSED,
1 by-design-accepted; 2 NEW-PROBLEMs.** The Profile-3 increment walk passed hard scrutiny
(derivationally correct against three-profiles.md's own citations). All five open items
fixed:

1. **NP1 (ch.11 ↔ ch.12 tension):** `brownfield.md` £34–40M counterfactual scoped to "the
   price of the missing *diagnosis*" + explicit pointer to Part IV's wall.
2. **NP2 (cross-cutting unfalsifiability):** `derivative.md` — pre-committed disconfirmation
   for "unforced = debt" (option value exercised → missing question); `two-teams.md` "prices
   them as debt" → "a price" so the strong claim lives only where the falsifier does.
3. **F1 (recovery smuggled premise):** `two-teams.md` recovery step names its input class
   (domain-shape facts, formalized in Ch. 2) at first use.
4. **F13 (Discord grade at point of use):** `derived-blind.md` cross-findings flag the
   C-grade where the finding is used.
5. **F19 (spine oversells ch.11):** `root.md` blurb "graded by" → "read retrospectively
   against."

Final tally: **37 manuscript edits, 12 files, 0.3.6 → 0.3.7.**

## ROUND 3 — deployment-axis artifact/unit refinement (user finding, 2026-07-15) — APPLIED (0.3.8)

**Finding (user, reading ch. 3):** the single-deployable cost line ("whole-system blast
radius and whole-unit scaling") is defeated by a real pattern — one artifact, handlers/process
classes enabled per instance group (event-driven handler activation; lineage: web/worker
processes from one codebase, Elasticsearch/Kafka node roles, HashiCorp single binaries).

**Analysis:** the entry silently conflates two countables — **artifact** (versioned/released
as one) and **runtime unit** (instance group shaped/scaled as one). Costs split cleanly:
release coupling, toolchain uniformity, dependency lockstep → artifact; scaling shape,
runtime blast radius, operational surface → units; network-in-crossing → boundaries the
composition actually crosses (largely pre-paid when the substrate is already event-based).
The user's pattern = the off-diagonal (one artifact, many units) = the unified-runtime value
hand-rolled, cheap because event composition made location transparency free. What it cannot
buy: release independence (one artifact = one train; runtime disable = kill-switch, i.e.
recovery machinery, not cadence); deploy-time blast (bad build reaches every pool);
dormant-handler wake-up trades isolation back (shared process envelope). Also corrects
"it is a young value": the *platform class* is young; the pattern is decades old —
strengthens the entry's operational record and shrinks the COI exposure (composes with B3).

**Validation-record check (no registered derivation flips):** Profile 3 read-path split also
cites cadence + 99.99% blast (survives); Profile 3 cores are direct-call coupled (what forces
the productized runtime over the hand-rolled form — the pick is now *better* cited); Discord
gateway is polyglot (artifact demand; survives); SO is explicitly "N identical instances"
(now the named baseline form); Profile 2 modular monolith unchanged. Ch. 1's preview walk
("what would demand a second deployable" + three demands) stays coarse — recommendation:
LEAVE ch. 1 (preview altitude, zero-pressure case, conclusion unchanged either way; ch. 3 is
the established refinement home for ch. 1's table). Boundary-price section: checked, no echo
needed (the "pre-paid at the substrate move" clause carries the interaction).

**Proposed replacement** — `axes-and-ledger.md`, the full **Deployment topology** entry
(one paragraph, matching the recovery entry's length; ~+190 words in ch. 3, the known
density-budget cost of the fix):

> **Deployment topology** — *single deployable / multiple deployables / unified runtime /
> serverless.* This axis prices two countables, kept apart because its costs split between
> them: the **artifact**, versioned and released as one — release coupling bills here — and
> the **runtime unit**, an instance group shaped and scaled as one — scaling shape and
> runtime blast radius bill here; the network inside a crossing bills to neither, only to the
> boundaries the composition substrate actually crosses, and is largely pre-paid where the
> substrate is already event-based. The single deployable — one artifact, identical
> instances — provides in-process calls on every internal path and one operational surface,
> at the cost of whole-system blast radius and whole-unit scaling; its modular form adds
> ownership boundaries at zero deployment cost, which is the fact that dissolves most "we
> need services" pressure once Chapter 2's decomposition has run. Its **role-selective
> form** — the same artifact, handlers or process classes enabled per instance group, a form
> with decades of record as web-and-worker processes from one codebase and database node
> roles from one binary — buys back per-role scaling envelopes and inter-pool blast isolation
> with no second artifact, at the price of the activation machinery becoming a mechanism of
> its own: role configuration, per-role capacity, knowing which instance runs what. What no
> single artifact buys is release independence: one artifact is one train, disabling a
> handler at runtime is a kill-switch — recovery machinery, not a cadence — and waking a
> dormant handler on a running instance takes the isolation back, one process envelope now
> shared. Multiple deployables provide what only artifact multiplicity can: independent
> release cadence, independent toolchains and dependency evolution, blast isolation that
> holds at deploy time too. Their cost is a network inside every crossing — a latency floor,
> partial failure as an internal concern, consistency across the boundary decaying from
> transaction to protocol — paid in full where calls are direct, and mostly pre-paid where
> the substrate move already put a broker between the parts. The unified runtime holds
> packaging open — one-or-many decided at deploy time without rewiring — at the cost of a
> platform dependency; the role-selective form is its hand-rolled ancestor, and what the
> product adds is the same deploy-time freedom for *direct-call* composition. The platform
> class is young; the pattern is not. This book's author builds one such runtime, disclosed
> here precisely so the derivations can be checked for favor: none requires it. Serverless
> provides per-invocation scaling to zero at the cost of cold-start tails and per-invocation
> pricing that crosses over against sustained load.

**Apply-time items:** (1) sync the deployment card in `appendix-reference-cards.md` (read
card first; carry artifact/unit split + role-selective form minimally). (2) Instruments tag
at ch. 3 foot: consider adding "the artifact/unit distinction" — or leave (it is an entry
refinement, not a new instrument; recommend LEAVE to keep the tag stable). (3) CHANGELOG
0.3.8 or extend 0.3.7 (recommend 0.3.8 — 0.3.7 is a closed, verified batch).

Per-file passes (each file opened once): `two-teams.md` (A1a), `axes-and-ledger.md` (A1b, B1,
B4, C-quorum), `judgment.md` (A1c, C-Allspaw, C-VOID), `brownfield.md` (A1d), `derived-blind.md`
(A2 ×3, B5), `derivative.md` (A3-i, A3-ii), `closing.md` (A3-iii, C-gap-drain),
`derivation.md` (A4), `root.md` (A4 spine), `verification.md` (B2, C-RTT), `three-profiles.md`
(B3, C-concession). Bump `CHANGELOG.md` to 0.3.7. Then: skeptic re-reads the changed A1/A2
passages to confirm the boundary statements close the gaps.
