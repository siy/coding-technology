# Handover — 2026-06-26 (rev: books published, site in polish)

## TL;DR — where things stand

- **Both books are PUBLISHED and live on Leanpub** (the prior cliff-edge action is done):
  - **JBCT 4.1.1** → https://leanpub.com/jbct-book (295 pages)
  - **PFD 1.1.0** → https://leanpub.com/process-first-design (120 pages)
  - Verified live via the API (`last_published_at` 2026-06-26). Full + sample editions both uploaded;
    readers were **not** emailed.
- **The site is rebuilt and served locally, but HELD (not deployed).** Open **http://localhost:8055**.
  Home reads "Version 4.1.1"; the web edition carries the latest ch02.
- **THE next action: the site polish pass, then deploy.** The user wanted to save site tweaks for last
  — adjust text/elements, possibly reorder sections — *before* it goes live. They had not started yet
  (they were choosing: hand me specific tweaks vs. I do a review pass + punch-list).
- **Deploy = merge `jbct-4.0-self-contained` → `main`** (Netlify deploys on push to main). Do NOT merge
  until the polish is done and approved.

---

## Git state

- Branch **`jbct-4.0-self-contained`**, pushed to `origin` (`git@github.com:siy/coding-technology.git`).
  **Not merged to `main`** (main untouched; merging deploys the site).
- Latest commits: `b3ff7c1` (PFD 1.1.0 + JBCT 4.1.1 + heading fix), `83b2ca9` (first handover), then
  this commit (README 4.1.1 sync + this updated handover).
- **Uncommitted = ONLY the Aether in-flight files** (`ai-tools/skills/aether-coder/*`, `book-aether/`,
  `book-aether-meta/`) — the **Aether-book-editor** session owns them; do NOT commit.
- PDFs/EPUBs are gitignored build outputs.

---

## Books — DONE (built, committed, published)

- **JBCT 4.1.1** — `book/CHANGELOG.md` top = 4.1.1; full PDF (6.3M) + EPUB (6.7M) + samples published.
- **PFD 1.1.0** — `book-pfd/CHANGELOG.md` top = 1.1.0; full PDF (632K, 120pp) + sample published. PFD
  **EPUB still deferred** (the TikZ seat state-machine diagram breaks EPUB). Section/subsection **font
  ladder fixed** in `book-pfd-meta/build-pdf.sh`.
- **How to (re)publish** — `./publish-leanpub.sh <slug> <pdf> [epub] [--sample] [--publish]` (key in env).
  Pattern used this session, per book: (1) full upload — answer `y`; (2) `--sample --publish` — answer
  `y` then `y`. Leanpub returns a **harmless "Unexpected Server Error"** on publish even when it
  succeeds; verify with
  `curl -s "https://leanpub.com/<slug>.json?api_key=$LEANPUB_API_KEY" | jq '{last_published_at,page_count_published}'`.

---

## Site — rebuilt, served, HELD; polish is the next step

- The branch already: renders the JBCT book at `/book/` (**27 pages** = 25 chapters + index + the
  changelog), dropped guide/series, fixed Tools/Services pages, reconciled all versions (home = 4.1.1).
- `website/dist/` is current (rebuilt after the 4.1.1 ch02 tweak and the README 4.1.1 sync).
- Preview: **http://localhost:8055** (a server instance from this session is bound there serving the
  fresh `dist/`; if dead, restart: `python3 -m http.server 8055 --directory website/dist`).
- **To finish the site:** do the polish pass → `cd website && npm run build` →
  `git checkout main && git merge jbct-4.0-self-contained` (this is the deploy).
- Review surfaces: `/` (home), `/book/` + a chapter or two
  (`/book/ch02-design-methodology.html`, `/book/CHANGELOG.html`), `/AI-TOOLING.html` (Tools),
  `/CONTACT.html` (Services).

---

## Pending / deferred (not done)

- **Site polish + deploy** — THE next step (see above).
- **Article** — `book-pfd-meta/article-service-boundaries-DRAFT.md` ("Service Boundaries Fall Where No
  Saga Crosses", trilogy capstone, plain-ASCII, "Löwy" correct). User is considering publishing it
  (dev.to, continuing his two saga articles). Move to `articles/` when finalizing.
- **Reviewer backlog (undrafted) in `book-pfd-meta/reviewer-feedback.md`:** Poltorak "Integrity at the
  Stateful Edge" + P1–P6, Hetland push-points, and **L4 concept map** (PFD foundations; TikZ/EPUB
  production caveat — start with an ASCII/box map). A future consolidated pass; NOT applied.
- **Yannick Loth exchange ongoing** — last reply sent was L3 (composition-not-nesting; he was
  rethinking his own IVP nesting model). Fold new replies into the "Yannick Loth" round in
  `reviewer-feedback.md`.
- **Race Condition Theater demo** (Hetland) — host as a site companion *after* an
  honesty/fidelity/attribution pass (memory `project_race_condition_theater_demo`).

---

## Conventions / gotchas

- **Never commit** the Aether in-flight files (`aether-coder/*`, `book-aether/`, `book-aether-meta/`) or
  PDFs/EPUBs (gitignored).
- **Commits:** single line, conventional prefix, no body, no trailers/attribution.
- **Plain ASCII** for any text the user pastes/publishes as his own (LinkedIn, articles) — but keep
  correct **diacritics in names/terms** ("Löwy", not "Lowy"; memory `feedback-avoid-em-dashes-in-sends`).
- **"The document" = the JBCT book** (`book/`); `CODING_GUIDE.md` was deleted.
- **Merging the branch to `main` deploys the site** — gated on the polish being approved.
- **Per-book versions** (`BOOK-VERSIONING.md`): JBCT **4.1.1**, PFD **1.1.0**.
- Build books non-interactively (`</dev/null`); the build scripts have trailing "open?" prompts.

## Key files

- `book/` (JBCT 4.1.1) + `book/CHANGELOG.md`; `book-pfd/` (PFD 1.1.0) + `book-pfd/CHANGELOG.md`
- `book-pfd-meta/reviewer-feedback.md` — collection log (Loth round, decomposition thread, deferred
  Poltorak/Hetland backlog)
- `book-pfd-meta/finding-change-drivers-DRAFT.md` (also now in PFD foundations),
  `book-pfd-meta/article-service-boundaries-DRAFT.md`
- `website/build.js`, `website/templates/page.html`; `publish-leanpub.sh`
