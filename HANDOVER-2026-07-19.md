# Handover — 2026-07-19 (session: pfd-editor — AS read support, review 2, 0.3.13→0.3.15, TRAIN SHIPPED)

## ⭐ RELEASE TRAIN FIRED AND COMPLETE 2026-07-19

User verdict "book looks fine, release the whole train." Executed end to end:
- **All three books LIVE on Leanpub:** PFD 2.1.0 (157 pp, 15:26Z; condensed = free sample),
  JBCT 4.3.0 (302 pp, 15:30Z, PDF+EPUB), AS 1.0.0 (86 pp, 18:56Z).
- **AS Leanpub slug = `architecture-synthesis-the-next-correct-step`** (user changed it
  mid-release from `architecture-synthesis`; the short book was browser writing-mode and
  the API rejects uploads for non-upload-mode books — the full-title book is upload-mode).
- **Repo:** PR #35 (train) + #36 (slug fix) merged --admin to main; tags arch-v1.0.0 /
  pfd-v2.1.0 / jbct-v4.3.0 pushed; **book-arch/ JOINED the repo** (manuscript committed;
  book-arch-meta/ stays private per never-commit); branches cleaned.
- **Site:** deployed to pragmatica.dev — new front door, AS Book button → new slug,
  nine-questions copy, 41 pages link-green.
- **JBCT stamp fix:** introduction.md + from-process-to-patterns.md 4.1.1→4.3.0 (user:
  stale). **Wording gaps fixed** (ch. 7 kit disclosure + references now match the live
  derivation-artifacts repo). **BOOK-VERSIONING.md** gained the AS row.

**LEFT FOR USER (announce only):** review copies → Poltorak + Yannick; nine-questions
pointer → Max Grom; announcement vehicle (article-2 "Three Famous Architectures, Derived
Blind" is publication-ready and parked, or a LinkedIn post — user's call). Fast-follows
NOT in train: RCT demo, AS course, PFD course, item-12 driver-modes-canonical.

**Lessons banked to memory (`project_release_workflow`):** Leanpub book must be
upload-mode before API accepts files; AS slug recorded; condensed = PFD sample; key at
`../.env-pub` (source it, mask `api_key=` in all output).

---

## Original session content (manuscript work leading to the ship)

Delta handover. For the release-train staging, JBCT/PFD state, and engagement register,
HANDOVER-2026-07-17.md remains accurate and is not repeated here. This session touched
ONLY: book-arch/ (4 files), book-arch-meta/ (3 new files), website/content/glossary.md,
memory. The train's gates are unchanged and still the only blockers.

## TL;DR

- **AS manuscript 0.3.15** (was 0.3.12). Three passes, all user-gated, all built+verified:
  - **0.3.13** — read-feedback item 3: ch. 4→5 "a derived vector is a set of claims"
    verbatim echo removed; definitional home = Verification (triplet maps onto its two
    checks), ch. 4 close trimmed to a lean hand-off. Same reader-wins ruling as 0.3.12.
  - **0.3.14** — read-feedback item 4 (user proposal): ch. 5 budget arithmetic
    restructured into **math → example + discussion pairs**. Law sentence verbatim + one
    set-off italic Unicode formula line + existing prose. Rules 1/2/4 full formulas;
    rule 3 rule-form; rule 5 inequality only (its own "comparison not formula" preserved).
    Rule 1's worked example is now the cold-open contract's own 90-second arithmetic.
    No LaTeX math mode — DejaVu Serif carries all glyphs, verified by pdftotext.
  - **0.3.15** — **external review 2 disposed and applied.** Review verbatim in
    `book-arch-meta/architecture-synthesis-review.md`; rulings + full before/after in
    `REVIEW-2-DISPOSITION.md`. Scores 9.2–9.8, "none fatal." User scope ruling:
    clusters A+B+D to 1.0; **C (worked tie, verify→correct→re-derive loop) + ch. 3
    diagrams deferred post-1.0** (register at the bottom of the disposition file).
- **Applied at 0.3.15:** ch. 4 new section "What the procedure is, mathematically"
  (constrained optimization over a chosen finite space; non-scalar objective → ties →
  refusals; global-in-space minimum citing the A4=C order argument — answers review items
  1/5/7/8/9); ch. 3 containment defined (ledger-discipline bullet: bound claim,
  probabilistic as its bound, dies at exit gate — item 4); axes count-is-output mirrored
  from ch. 2 (item 2); ledger schema articulated, requires/excludes declined in-text
  (item 3); recovery asymmetry owned ("nothing requires the dimensions to be the same
  kind of thing" — item 6); ch. 1 press/inert terms-of-art (editorial). Site glossary:
  press + inert entries added, Persistence Configuration entry scoping aligned.
- **Rejected with evidence** (in disposition): "mechanically" (4 occurrences total, not
  frequent — reviewer perceived the ~60-strong *mechanism* family, which is the core
  term), "standing bill" (2 occurrences), reviewer's suggested PFD open-question pointer
  (**no such PFD passage exists** — verified; AS states the empiricism directly instead).
- **Correctness fix** (review's item, applied): "the *only* value **in this ledger** that
  provides strict transactions across regions…" — axes-and-ledger.md + site glossary;
  three-profiles.md already had the scoped form.
- Builds green at each step. Current: **draft 86 pp / sample 28 pp**, 0.3.15 stamped,
  all six new-content probes verified in draft, both sample-spine probes in sample.

## User rulings this session (beyond the review scope ruling)

- **Voice stays.** "Too native for non-native readers?" raised as a question, assessed,
  settled: argument layer is L2-recoverable (terminological discipline, formula lines,
  reference cards as plain-restatement layer); pleasure/persuasion layer is
  native-tuned and that trade is accepted (user: "I like it"). If non-native reader
  feedback ever warrants action, the lever is **splitting the longest periodic
  paragraphs** (deployment-topology entry first), never vocabulary/idiom. In memory.
- User's read of the week's direction: the 0.3.14/0.3.15 changes move the book toward
  "less art, more engineering" in the layer traditionally run on taste — this framing
  may be worth reflecting in the front-door AS card copy (prepared strings in
  RELEASE-TRAIN.md predate this week's pass; suggested a look before the train fires).

## ⚠ Open items (this session's additions to the gates)

1. **Replication-kit wording vs reality** (found at session end, NO ruling yet):
   - The internal registration files EXIST: 8 `BLIND-*` files in book-arch-meta/,
     dated Jul 6–11 — the "dated working files" derived-blind.md:15 attests.
   - The public repo **github.com/siy/derivation-artifacts is live and substantially
     populated**: all four runs with README/ANSWER-SHEET/REGISTERED-PREDICTIONS/GRADES/
     SOURCES; Companies House additionally DERIVATION-TRANSCRIPT + OPERATOR-PROMPTS;
     protocol/ = PROTOCOL, GRADING-RUBRIC, EVIDENCE-GRADES; counterexample issue
     template; schema/ = summary README (full spec pointer: pragmaticalabs/pragmatica#443).
     Repo README handles attested-vs-prospective honestly and documents the
     incremental-population policy.
   - **Gap A:** derived-blind.md:15 still says the kit "**is being prepared**" while
     references.md:46 cites the live repo — ch. 7's disclosure lags the book's own
     references. Should become "published at the repository the references cite" (or
     similar) at/before ship.
   - **Gap B:** references.md:46 claims "run transcripts for the graded runs" but only
     CH has a transcript in the repo (3 of 4 missing; the internal BLIND-* files are the
     source — needs a scrubbing pass, or the references line softens to match).
   - Decision owed: populate the three transcripts vs. soften the references line.
2. **Ch. 5 intro clause** "None requires more than arithmetic — four of them not even
   that much" — flagged twice (sits oddly next to the now-visible formulas; honest form
   ≈ "one of them only counting"). User has not ruled. One-line fix if approved.
3. **The three train gates + JBCT stamp verify** — unchanged from 07-17 handover:
   (a) AS read verdict (the trigger), (b) Leanpub slug `architecture-synthesis`,
   (c) book-arch tracking at ship, (d) JBCT intro "Based on: JBCT v4.1.1" stamp check.

## Working-tree note

New uncommitted this session: `website/content/glossary.md` (press/inert entries +
persistence scoping) — tracked file on site-redesign, batch with site work at wrap-up.
book-arch/ + book-arch-meta/ changes stay in the never-commit set until the tracking
decision (gate c). Pre-existing aether-coder + PLANNED-CHANGES working-tree edits
untouched (see 07-17 handover).

## Conventions & lessons (additions)

- **Verify reviewer claims like API claims** — extended the verify-against-source rule
  to review feedback: this review's two frequency complaints failed grep, its PFD
  pointer failed existence check, and its misquote of the "only value" sentence hid the
  fact the book was half-right already. Ground-check before drafting dispositions;
  rejections-with-evidence are as valuable as accepts.
- **Repetition rulings compound**: 0.3.12 (null vector), 0.3.13 (claims hook) — the
  operative rule is now stable: *the definitional home keeps the statement; the other
  site cites or trims; reader experience overrides structural symmetry.*
- Assessment on record (mine, for the next fresh-read pass): ch. 4's back half is now
  three reflective sections in a row (refusals → mathematically → hand-off) — pacing
  worth one skeptic look; the bolded global-minimum claim is now the book's most
  formally exposed statement and the deferred worked-tie example is its natural
  evidence — an argument against letting Cluster C slip far past 1.0.
- Memory updated: `project_arch_book` (0.3.15 state block + voice ruling), description
  line current.
