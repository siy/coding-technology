# Probe results — 2026-07-17 (book-as-QA loop, ticks 2 + 3)

Delta-driven tick after the rc2 release: baseline **HEAD `d9c51316b`**, branch
`release-1.0.0-rc3` (rc2 shipped end-to-end incl. history rewrite + Maven Central;
826-file format sweep #447 — **tick-1 line numbers are stale; symbols hold**). Four
agents: new contract-docs wave / durable-entity re-check / on-ramp+almanac recon /
examples+Central+old leads. All GitHub-bound findings hand-verified.

Runtime-side outcomes: filed **#468** (default-stream delivery labeled at-most-once AND
at-least-once in one commit, scope split unstated), **#469** (guarantees.md behind its
own wave: gap #7 unclosed, stale grounding line, suspected #277→#352 typo), **#470**
(RateGuard SPI author-facing + undocumented), **#471** (thinking-in-slices cites retired
CODING_GUIDE.md). Mailbox entry 2026-07-17 22:14 incl. the open question: which DHT
consistency config backs the `dht.epoch-gate` keyspace (entity-fence substrate) —
FULL/eventual or quorum? Awaiting aether-main; file or drop on answer.
Tick-1 issues: #432, #433 unchanged, correctly parked (#432 waits on the #345 rc3 kickoff).

**Everything below is BOOK-SIDE and GATED** (prose pause of 2026-07-04 still in force).

---

## REVISION to tick-1 punch list (PROBE-2026-07-09.md)

**Item 7 (Module D banner) — the correction now runs the OTHER way.** Tick 1 said
"per-key serialization landed, soften the banner." Tick 2 ground truth
(guarantees.md row 28 + §6, known-limitations.md, 14-consistency-and-partitions.md, all
2026-07-17): the entity module's classes exist with tests, but **the resource is not a
dependency of aether/node — no deployed slice can inject ANY durable entity**, and the
wired impl is in-memory: no fence, no replication, no durability, no owner-routing in
production. The book's D.2 claims ("the fence an entity rests on works today, so its
state is held across the configured replicas and survives the loss of a node: that is
high availability", ~1226-1227; echoed ~1004, ~1483 "highly available today") are
**overclaims, not underclaims**. Correct framing when the gate lifts: "designed and
pinned; module code exists with tests; NOT injectable in a deployed slice yet; neither
HA nor restart-durable in production today" — cite #349 + guarantees.md §6. This
SUPERSEDES tick-1 item 7's direction; per-key-serialization wording stays true at the
module level.

Line numbers in PROBE-2026-07-09.md predate the format sweep — re-locate by symbol/quote.

## NEW P1 — teaches something false

19. **Module D "highly available today" overclaim** — see revision above. (This is the
    tick-2 headline for the book.)

## NEW P2 — materially incomplete

20. **Routing chapter silent on shipped #339/#198 schema** — and there is now a LIVE
    example to teach from: `examples/catalog` routes.toml uses `[api]`,
    `[v1.routes]`/`[v2.routes]`, `deprecated`/`sunset`, per-route `produces`/`consumes`
    (csv, octet-stream); its javadoc names itself living documentation for both
    features. 13 other examples remain flat-legacy — the book can show both forms.
    Upgrades tick-1 note "examples pending": they landed.
21. **Part II can now cite real Maven Central coordinates** — `org.pragmatica-lite:*`
    at **1.0.0-rc2** verified reachable (core, jbct-maven-plugin, slice-annotations,
    slice-api, resource-db-jooq). Cite rc2, NOT rc3 (unpublished). First time the book
    can pin real coordinates.
22. **Module D should cite #349 explicitly** for the persistence gap and add a
    forward-note: once entities ship, `StaleOwnerEpoch` / `PartiallyCompensated` /
    `RUN_ONCE` marker each owe a Failure-Almanac row (almanac's own maintenance rule).
23. **Part V 5.5 sourcing**: adopt the Failure Almanac's field set (Symptom / Detection /
    Automatic response / Budget / Degraded-at-risk / Operator action / Proof anchor) as
    the budget vocabulary — it's test-anchored and Aether-specific. CAUTION: the
    "50% rule" phrasing exists NOWHERE in runtime docs — that framing is the book's own
    and needs its own derivation or a rename.

## NEW P3 — polish

24. **Part 0: one cross-reference line** to `aether/docs/slice-developers/thinking-in-slices.md`
    (runtime's own functional-model primer; no vocabulary drift — four shapes + six
    pattern names align; it lacks the knowledge frame, the thesis, and the DI split, so
    Part 0 keeps ownership).
25. **Module B stream chapter**: RF=1 disclosure (tick-1 P2.9) now has first-class
    sources to cite: known-limitations.md ("one-disk-deep", #262 two-knob) +
    failure-almanac RF=1 owner-failover row + 14-consistency table. Delivery semantics:
    teach the **three-regime split** per #468's final ruling (CLOSED 2026-07-17, in both
    docs, consumer-path-verified via `ConsumerRuntimeState` WAL-rebuilt ring):
    (a) live session = at-least-once (un-checkpointed window redelivered);
    (b) restart with intact WAL = replay-from-0, duplicate-heavy, still at-least-once;
    (c) genuine at-most-once ONLY where the log is lost (RF=1 owner failover /
    fresh-disk replacement). NOTE: my tick-2 two-scope framing (cursor loss ⇒
    at-most-once) was too coarse — superseded by (b).

## Verified clean this tick

- Durable-entity code + spec byte-stable since tick 1 (spec still v0.3.0; ReadConsistency
  surface unchanged; zero workflow/saga/timer/signal code; #350-354 untouched, rc3).
- Book vs new docs wave: pub-sub at-most-once, stream per-partition ordering, KV-fence
  scoping all CONFIRMED consistent.
- H2/multi-dialect migration splitter (#409) does NOT contradict Module A's
  external-Postgres local-dev story (H2 is testing-only per resource-reference).
- thinking-in-slices poses no ownership threat to Part 0; Failure Almanac corroborates
  Module D's honesty notes (zero contradictions).

## Leads for tick 3

- ~~`dht.epoch-gate` keyspace consistency question~~ **RESOLVED same evening**: aether-main
  answered (mailbox 22:13) — not a DHT keyspace at all; authority = consensus KV
  (`OwnershipEpochHighWater` seeded/advanced from committed KV), enforcement =
  engine-local applied high-water (`HighWaterOwnerEpochGate` at `AetherNode.java:420-421`,
  enforce-at-replica, transient window = local KV apply lag). **Filed as #472** (code-verified).
  Book consequence: Module D's fence story can state the fence inherits KV guarantees,
  NOT the #384-downgraded eventual DHT semantics — cite #472's row once it lands.
- #468/#469 resolution wording → feeds Module B + any guarantee citations (all four
  tick-2 issues ACCEPTED, fixes in flight per aether-main 2026-07-17).
- #432 resolution at #345 rc3 kickoff → unblocks punch item 8 (Module D error names).
- **1d stream-append fence (probe candidate handed over by aether-main, 22:30):** verify
  whether the stream-append fence consults `OwnershipEpochHighWater` or a stream-specific
  ownership high-water, then pin 1d's wired status in the `OwnershipEpochHighWater`
  javadoc (they left it status-neutral rather than assert unverified mechanism).
- Watch for rc3 Central publication (book coordinate bump rc2→rc3 when it happens).
- Cloud-provider wave (RFC-0016 landed with owner rulings Q1-Q6; W1 landed 2026-07-17
  `b9ef6c475` — all provisioning through static `ProvisionRequest.resolve`; AWS spot arm)
  is reshaping Part V's substrate. Fresh probe surface per aether-main: `docs/rfc/RFC-0016-*`,
  the reference wave, rewritten vm-snapshot mechanism. Probe before drafting 5.1/5.2.

## Decision-E note (for Sergiy, not the loop)

rc2 released ⇒ decision E (Aether-book sequencing) is live per the 07-04 agreement.
Tick-2 facts that bear on it: entity/workflow/saga remain unstarted (#350-354, rc3);
the runtime's doc surface (guarantees/known-limitations/almanac/consistency contract)
matured a lot — Modules A-C + the guarantee-honesty spine of the book can now cite
stable, test-anchored sources, while Module D's runtime substrate is still moving.

---

# TICK 3 (same day, evening) — baseline HEAD `2a8a2f80e` (the tick-2 drain batch)

Three agents, rolling filing per Sergiy's standing order. Issues: **#473-#477**.

## Runtime-side outcomes

- **#473** — 1d stream-append fence VERIFIED WIRED (aether-main's handed-over question):
  `StreamPartitionManager.rejectIfStale` + `OwnershipDomain.streamPartition` against the
  SAME `OwnershipEpochHighWater` as the DHT gate; covers local-publish + replica-receive;
  read fence (`LinearizableOwnerServe`) consults identically; `guarantees.md:116` already
  accurate. Issue = javadoc pin with drafted wording.
- **Tick-2 drain verified 4/5 complete** (#469 #470 #471 #472 — RateGuard doc claims all
  check out against source). Residual → **#474**: guarantees.md §4 still claims periodic
  cursor checkpoints; ground truth: `CursorStore` is an extension seam (`StreamAccessFactory
  .java:85`) that `AetherNode` NEVER registers; `PgCursorStore` exists unwired; production
  restart = replay-from-0 (regime (b) of the three-regime split confirmed at code level).
- **Cloud wave, first probe: #475** (RFC-0016 internal drift: matrix pre-W1; ON_DEMAND
  acceptance criterion false at HEAD — dead field, market derived from role; §2.4
  misdescribes #459), **#476** (vm-snapshot.md cites deleted `resolveImage()`, frames
  landed W1 parity as pending), **#477** (feature-catalog rows 4/204 carry claims the
  RFC's own Motivation ruled false; W9's replacement wording never applied).
  W1/W7 CODE verified clean vs commit messages.

## Book consequences (all gated; add to the punch queue)

26. **Modules B/D: ONE fence mechanism to teach.** Both data planes (DHT writes, stream
    appends) + the linearizable read gate consult the same consensus-derived
    `OwnershipEpochHighWater` (domain-parameterized). Teach once, not per-plane. (#473 evidence.)
27. **Module B honesty paragraph, cursor half — VERDICT IN (#474 resolved 2026-07-17 23:22;
    all three disputed claims were partial, incl. mine).** Final published truth to teach:
    **no automatic cursor resume on a production node.** Split by audience: (a) app
    `StreamAccess` consumers — bounded-window redelivery ONLY via explicit
    `committedOffset()` commit + re-seek `fetch(K)`; at-least-once, same-node restart,
    writable data dir; (b) system consumers + Forge + unwritable mounts — replay-from-0;
    (c) the auto-resuming `ConsumerRuntimeState` path is TEST-ONLY. `AetherNode:5114`
    DOES register the CursorStore extension (my "never registered" was the truncated-grep
    error); registration ≠ auto-resume. **#478** (new) tracks the dropped A6 resume-at-K
    leg + owner ruling auto-vs-app-driven + missing restart test — watch it before
    finalizing the chapter; cite the revised #468 regime-(b) docs once the cascade hash
    posts (aether-main will mailbox it).
28. **Part V provisioning model settled enough to plan against:** one provider-agnostic
    shape — `ProvisionSpec` → static `ProvisionRequest.resolve()` (spec > provider config >
    loud fallback) → `createFrom` per provider. AWS spot arm real (`SpotMarketOptions`);
    Hetzner rejects SPOT loudly. NOT settled: W3 persisted SourceProfile (Hetzner
    prefix-listing SSH fallback still live — replacement-node provisioning is NOT fully
    KV-driven yet), W4/W6/W2 unverified. `AETHER_CLOUD_PROVIDER` is test-harness-internal —
    do NOT teach as operator config.
29. **Part V sources hygiene:** vm-snapshot.md (#476) and feature-catalog rows 4/204 (#477)
    are unreliable until their fixes land — draft 5.x only from RFC-0016-post-fix + code.

## Loop-health note (worth a line in the next handover)

Same-day doc waves drift against same-day code waves (W9 doc vs W1 code crossed mid-air;
the #468 fix touched 2 of 3 cross-referencing docs). The probe's niche is exactly this
cross-wave reconciliation — file fast, while the authors still have context.

## Tick-4 leads (handed over by aether-main, 2026-07-17 23:06 — all 5 tick-3 issues accepted)

- **W2 (per-role image) + W10 (spot validator) landed AFTER tick-3 HEAD** (`e472a373f`):
  vm-snapshot per-role note and PF-16's data-driven set are already newer than the tick-3
  probe surface. Re-verify #476/#477 fixes against the NEWER state, not tick-3 assumptions.
- **#474 cascade committed at `c48ae35d3`** (2026-07-17 23:27): guarantees §4 +
  known-limitations + 14-consistency regime-(b) split + catalog row 185 + plan A4/A6
  divergence markers — all four contract docs now state the per-path cursor truth
  identically. Tick 3 fully drained 5/5; #478 carries the contract ruling + missing
  restart test. Punch item 27 cites THIS hash's wording; include cascade re-verify in
  tick 4's sweep (pattern: tick-3's verify pass caught a tick-2 residual).
- **W6/W3/W4 in flight tonight** (formatVersion gate, persisted SourceProfile,
  cleanup-credential unification) — stored-format behavior is new ground, richest tick-4
  surface. NOTE: W3 landing invalidates punch item 28's "W3 NOT landed / replacement
  provisioning not fully KV-driven" — re-verify before Part V planning relies on it.
- Probe AFTER that wave lands (probing mid-wave = churn); aether-main will have signaled
  or the commits will show it.
- #475 resolution direction: RFC criterion updates to as-built (ON_DEMAND literals =
  deliberate inert dead fields). When it lands, punch item 28's provisioning-model wording
  stays valid; cite the updated RFC section.

---

# TICK 4 (2026-07-18 ~00:25) — baseline `7f9130338` (stored-format wave W6+W3+W4); HEAD moved to `f7f52c0f0` (RFC as-built final) mid-probe

Two agents (stored-format probe + doc-cascade verification), rolling filing. Issues
**#479-#482** + comment on #444. (Mailbox entry mis-timestamped 00:55; actual ~00:24 —
append-only transport, left as-is.)

## Runtime-side outcomes

- **#479** RFC-0016 W6 cites dead code ("both parse sites" — `parseFile`/`validateConfigVersion`
  has ZERO callers, exhaustively verified; one shared gate site; CHANGELOG:10 already correct).
- **#480** cluster-bootstrap-spec REQ-3.2.3 ordering false on live path (template resolution
  before version check) + REQ-3.2.1 error-text mismatch.
- **#481** destroy orphans bootstrap SSH keys (no teardown; orphans re-matched by prefix
  listing on same-name recreate).
- **#482** cascade enumeration nit (known-limitations regime-(2) omits unwritable-mount;
  guarantees.md:115 names it).
- **#444 comment** (aether-main's requested independent look): collision broader than
  prod/prod-eu — bare `startsWith`, no boundary char, ANY string-prefix pair collides
  (prod/production, web/webapp); interim fix = match `prefix + "-"`; only vulnerable site
  repo-wide (cleanup deletes by exact id; other providers have no listing); provider javadoc
  overclaims ("never resolving another cluster's keys" — false as written); `SourceProfile`
  persists neither key id nor name.
- **Tick-3 fixes verified 5/5 COMPLETE** (incl. #476/#477 correct against post-W2/W10 state;
  RFC as-built final `f7f52c0f0` internally consistent — the docverify agent's one suspected
  contradiction was its own read-cache artifact, self-diagnosed). W6's four gate cells tested
  as claimed; W4/#439 negative-assertion test real; W3's account-wide fallback genuinely deleted.

## Book consequences (gated, punch queue continues)

30. **Punch item 28 update:** W3/W4/W6 LANDED. Replacement provisioning is KV-driven EXCEPT
    the one Hetzner ssh-key prefix listing (#444 remainder). Part V can now teach:
    config_version exact-match gate (named errors, single shared parse boundary — cite
    #479's corrected wording once landed), cluster-scoped ssh keys, unified handle-first
    cleanup credentials. The #439 negative-assertion test (`assertFalse(envReads.contains(
    "HCLOUD_TOKEN"))`) is a ready-made Part IV example: testing what must NOT happen.
31. **Part V spot story now data-driven:** W10 validator (`SPOT_UNSUPPORTED_REASONS`, only
    AWS absent) + real AWS `SpotMarketOptions` arm + Hetzner loud reject. Catalog row 4
    wording now accurate — safe to cite post-tick-4.
32. **Module B cursor paragraph:** cite guarantees.md §4 at `f7f52c0f0`+ (per-path truth incl.
    app-path disk-durable cursor via `AetherNode:2861/:5114` and the non-atomic `replaceRef`
    caveat). #482 one-word fix pending; #478 (resume-at-K ruling + restart test) is the
    watch item before the chapter finalizes.

## Loop scoreboard after tick 4

17 issues filed over 4 ticks (#432-433, #468-482), 11 closed, 2 parked (#432/#433),
4 fresh (#479-#482), #478 on their side. The verification layer has now caught: a tick-2
residual (#474), a both-sides-wrong dispute (#474 verdict), and confirmed tick-3 5/5 clean.

**Tick-4 DRAINED 5/5 (aether-main 2026-07-18 01:15, commits `6d4c6ea75` + `bef6b65fa`):**
#479 dead parseFile deleted / #480 gate now FIRST on live path + spec REQs literally-true /
#481 destroy sweeps keys (handle-first, delimiter-bounded) / #482 closed / #444-interim
prefix+"-" at BOTH lookup and sweep + javadoc weakened. **Final 4-tick scoreboard: 19 filed,
18 closed same-night, 1 rescoped into #444's remainder. Full-cycle latency under an hour.**

## Tick-5 surface (when warranted — do NOT tick immediately; thin until W5 actually runs)

- Verify tick-4 fixes (`6d4c6ea75`, `bef6b65fa`) — the established verify-the-drain pattern.
- **W5 LocalStack contract suite landed** (`38ad2dd00`, docker-gated) — but its FIRST DAEMON
  RUN hasn't happened; the probe is read-only and can't execute it. Probe value appears
  AFTER a run exists (results vs claims) or when its docs/catalog rows land.
- #478 (resume-at-K owner ruling + restart test) — Module B watch item.
- #444 remainder (exact-id matching, firewall refs, labels policy) — Module B/Part V watch.
- #432 at #345 rc3 kickoff — Module D error-name unblock.
