# Handover — 2026-09-15 (session: pfd-editor)

Continues `HANDOVER-2026-09-12.md`. **Three PRs merged (#75, #81, #82), two open (#83, #84), one issue
closed (#60).** `main` is `3bfb379`; both open PRs are green and based on `main`.

Versions unchanged and unpublished: JBCT **5.0.0**, PFD **3.0.0**, AS **1.1.2**, Aether 0.1.0 draft. No tags.

---

## Read this first: four things that will bite a successor

**1. The 09-12 handover's Netlify blocker is GONE, and the way to check is the status API, not the PR.**
It said the disconnect "had not taken effect". It took effect between 14:00Z and 16:41Z on 2026-09-12:
#77's head still carries a `netlify/jbct/deploy-preview` status, and #78, #79, #80 carry **none**. The four
red Netlify checks that sat on #75 were dated **2026-09-11** — residue on a head that had not moved, not a
live failure. Read `gh api repos/siy/coding-technology/commits/<sha>/status`, because the PR rollup renders
a `StatusContext` with `name: null` and hides its description.

**2. `--admin` is not needed and `CLAUDE.md` was telling you otherwise.** Ruleset 9648960 is scoped to
`refs/heads/main` with `required_approving_review_count: 0`; plain `gh pr merge --merge` works, verified
three times this session. Corrected in #84. **The installed `/release` skill still says "Branch protection
requires admin" — and it is NOT tracked in `ai-tools/`**, so `check-drift.sh` cannot see it. Five of the ten
installed skills (`release`, `article-cross-poster`, `config-snapshot`, `consistency-lens`, `premium-ui`)
are outside the drift net. That is how a false instruction survived for days.

**3. The retention rule is written but NOT yet in effect, because #84 is unmerged.**
`CLAUDE.md` (in #84) says the root keeps the current handover and its immediate predecessor. Once #84
merges, root holds `09-09` + `09-12`; adding this file means **`HANDOVER-2026-09-09.md` moves to
the private record in the same operation.** It was deliberately NOT moved now:
chaining a second unmerged PR onto an unmerged one creates a half-state for no gain. Do it when #84 lands.

**4. Presence is not agreement — this cost me the one real miss of the session.** I concluded the May-June
drafting conventions were "already homed" because the private authoring specs covered the same topics.
They cover them *and disagree with them*: one still prescribes the threads-advanced tag "at the chapter
foot", which 2026-06-15 revoked, the shipped book has **zero** occurrences of, and
`book-pfd-meta/thread-coverage-map.md` records as stripped. Nothing was stranded — but two durable homes
contradict each other and the stale one is what `CLAUDE.md` names canonical for voice. When you check
whether a rule is homed, check that the home *agrees*, not that it mentions the subject.

---

## What shipped

| | |
|---|---|
| **#75** | The pandoc site migration, unblocked and merged. Built the merge with `main` before merging: 1,414 anchors identical, 580 code blocks identical, `_headers`/`_redirects` byte-identical to main's, 170 tests green |
| **#81** | `main` went red the moment #75 landed — #76's pin guard (merged after #75's last push, so #75's own CI never ran it) found `Pragmatica 1.0.0-rc1` used as a slug fixture. Renamed to `Pandoc 3.8.3-rc1`; mutation-probed to confirm the row still pins something |
| **#82** | Teaching order C — your ruling `know:` `4607e86d0`, committed 2026-09-08 and never opened as a PR. Found by sweeping unmerged remotes |
| **#60** | Closed. #73 and #76 delivered it |
| **#83** | **OPEN, for review not merge.** The Aether-book-editor stream's 11 commits from 2026-09-02/03, never opened as a PR. Reviewer's brief is in the body |
| **#84** | **OPEN.** The handover consolidation — rules given homes, 28 files archived |

Also: 6 merged worktrees and 1 dead one removed, 7 local branches deleted, skills/agents re-installed
(`check-drift.sh` exit 0), and a config snapshot taken before the reinstall.

---

## The handover archive (#84)

**28 of 30 root handovers moved out of this repo into the project's private record**, with an index,
agreed with the maintainer of that record before anything was written. 4,449 lines out of a public repo. The seven `book-*-meta/` handovers **stay** — own citation chain, one cited
by a live verification doc, and `CLAUDE.md` already designates `<book>-meta/` as their home.

**The extraction's result was mostly negative, which is the finding.** Rulings had been routed to their real
homes when made, so the handovers were a duplicate record: **15 of 15 owner rulings already homed** — in the
books, `BOOK-VERSIONING.md`, or project memory. What had no home and is now written down: the `articles/` draft-status
rule, "published work is history", private-material-in-records, the zsh gotchas, the retention rule; plus two
memory files (delegation stall, widen-don't-add-beside).

**Deliberately dropped, stated rather than silent:** "documenting an asymmetry is a complete answer"
(situational) and "prefer positive formulation" (already carried by the voice docs).

**Three references repointed** so nothing dangles: `HANDOVER-2026-09-09.md:3`,
`book-pfd-meta/HANDOVER-synthesis-spec-2026-05-28.md:64`, and `website/build.js:955` — the last cites
**PR #34** instead of a handover, which a public reader can actually follow. All three land with #84; on
`main` today `build.js:955` still names the archived file.

---

## Open

**Needs you, outside any repo:**
- **The `/release` skill's stale `--admin` line** (and "Netlify auto-deploys on merge", also wrong now).
  A `~/.claude` edit; snapshot already taken.
- **One private authoring document disagrees with the shipped book** on a convention revoked in June
  (see trap 4). Owner-authored and outside this repo, so it is the owner's to reconcile.
- **The `audits` discussion category** still does not exist; the template merged 2026-09-09 stays inert.

**In this repo:**
- **#84** — merge, then move `HANDOVER-2026-09-09.md` to the archive per trap 3.
- **#83** — needs a reader-not-author pass over 645 lines of unreviewed prose, a list of which rc3 stamps
  rc4 would move, and a decision on whether the public copy of `aether-book-voice.md` should carry its edits.
- **#61** genuinely blocked: it exists to quote pragmatica #872/#873, both still open.
- **#16** open (jbct-maven-plugin 2D-array parse error).
- **Unhomed, deliberately not written down:** "JBCT prose uses spaced hyphens, PFD/AS use em-dashes" — a
  quick count (JBCT 410 em-dashes, PFD 1,042) does not confirm it. Needs your ruling first.
- **Unhomed, minor:** "show, don't tell — applied to status updates, not just technical claims".
- Two local branches survive that `-d` cannot delete: `fix/deployed-site-headers` (squash-merged as #78) and
  `release-2.0.12` (one commit, 2026-01-05, no PR).

---

## Working notes

- **Five instrument failures, all mine, none caught by re-reading.** A personal-data grep that **examined
  nothing** — unquoted `$FILES` collapsed to one argument, and the "0 words" count was the only tell. A
  homed/unhomed probe where `stall` matched **install** and manufactured a HOMED verdict. A saga-ruling grep
  in `book-arch/` when the chapter lives in `book-pfd/` — the right pattern, a space that could not contain
  the answer. A `grep -rn pirsch` "live trap" that `CHANGELOG.md:53` had already documented and solved. And
  **"7 of 7 owner rulings"**, a ratio computed over a set defined by a *heading* rather than by content; the
  real population is ~15. Each was caught by a count that looked wrong, never by reading my own work again.
- **The delegation did not pay off, and that is now a memory entry.** Four extraction agents went idle
  without delivering, and did not answer a direct request for their reports either. I read the sections
  myself using line numbers I already had. Their reports arrived **after** the work shipped — 177 rule
  entries, more thorough than my reading, and they found the trap-4 miss and the 7-of-7 overclaim. The July
  2026 handover recorded this exact pattern; two independent occurrences is what makes it durable rather
  than a bad day. Keep a direct path to any answer you delegate.
- **A second reader is not redundant effort — it is the only thing that caught either error.** Both
  corrections came from the parallel extraction reading the same artifacts, not from me re-running my checks.
- **Verify a merge by building it, not by `merge-tree`.** #75 and #78 both rewrote `website/build.js`;
  the textual merge was clean and told you nothing about whether the merged renderer still emits `_headers`.
