# Handover — Aether book (2026-07-12, session "Aether-editor")

For the next session continuing the Aether book. **Supersedes `HANDOVER-2026-07-08.md`.**
Read order: `.claude/scratch/aether-book-editor-brief.md` (onboarding) → `BOOK-PLAN.md`
(authoritative design + roadmap; carries a dated 2026-07-09 probe correction in §4b) →
**`PROBE-2026-07-09.md`** (the punch list — now the central working doc) → this handover.

**Session identity:** you are `Aether-book-editor`. Lane: `book-aether/`, `book-aether-meta/`,
`ai-tools/skills/aether-coder/*`. Repo-root `HANDOVER-*.md`, `website/*`, `book/*`, `book-pfd/*`,
`book-arch*/`, `articles/*` belong to other sessions — never commit them. NOTE: the repo-root
`HANDOVER-2026-07-08.md` is the *other* session's ("The Editor", architecture book) — the
Aether-book handovers live HERE in `book-aether-meta/`.

## ⚠ Session-start actions

1. **Re-arm both monitors** (they die with the session; both are dead now):
   - Mailbox (poll mtime every 60s, persistent):
     `f=~/IdeaProjects/pragmatica/aether/docs/internal/coordination/MAILBOX.md` — emit an event
     when `stat -f %m` changes. Read anything new first; protocol: silence = no exceptions.
     Last entries: Editor PROBE TICK 1 (07-09 23:26), aether-main CONTRACT AMENDMENT (07-10 09:20).
   - Cross-session channel (`pfd-editor` counterpart, handles case-sensitive):
     `tail -n0 -f .claude/scratch/cross-session.md | grep --line-buffered -F -- '-> [Aether-book-editor]'`
2. **Check rc2 cut:** `gh issue list -R pragmaticalabs/pragmatica --milestone "v1.0.0-rc2" --state open`
   — was down to **1 open (#427)** on 07-12. When rc2 cuts, **decision E (Aether-book
   sequencing) goes live** — surface it to Sergiy.
3. **Check the gap queue:** `gh issue list -R pragmaticalabs/pragmatica --label aether-gap --state open`
   — #432/#433 were open, triaged rc3, on 07-12.
4. **Ask Sergiy whether the prose pause still holds** before touching book text.

## TL;DR — where things stand

- **The "book as QA program" loop is LIVE and has completed one full cycle.** Probe tick 1
  (2026-07-09): whole drafted surface (Parts I–II, Modules A–D) verified against rc2 HEAD by
  5 parallel read-only agents. Filed **#432** + **#433** (label `aether-gap` — I had to CREATE
  the label; it didn't pre-exist despite the contract). aether-main triaged both to **rc3**.
  **#382 CLOSED** after my verification comment; #363 updated (processor half landed,
  category is `pg-notification` not `pg-notify`) and re-labeled rc3.
- **18 book-stale items, prioritized P1–P3, in `PROBE-2026-07-09.md`** — execution-ready for
  the gate lift. ZERO prose was edited (pause honored). Meta-docs updated instead
  (BOOK-PLAN §4b correction + error-glob CONFIRMED note).
- **Prose remains PAUSED** (2026-07-04 decision, unlifted as of 07-12).
- Book volume unchanged: ~16k words, 29/49 sections drafted, all uncommitted/untracked.

## What probe tick 1 established (pin these; each verified at source, 2026-07-09)

- **Aspect factory seam is DELETED** (`fd1649ad0`, closed #277): generated factories are
  single-arg `(SliceCreationContext)`; `SliceFactory` rejects 2-arg. Part I teaches dead code
  (punch list P1.1). User interceptors are **compile-time by RFC-0008 design** — runtime
  switching is a declared non-goal (`runtime-observability-aspects-spec.md:89`); what IS
  runtime-switchable: system observability via `/api/aspects` (ObservabilityStrategyCells).
- **Error globs match the Cause type's SIMPLE NAME only** (`ErrorTypeDiscovery.java:157`) —
  never messages, never enum constants; #385 (CLOSED) ships compile-time unmapped-Cause
  build failure + zero-match warning. Book's `*PAYMENT_DECLINED*` example can never match
  (P1.2). BOOK-PLAN "unconfirmed" item settled.
- **`publish()` awaits every subscriber** (`TopicPublisher` request-response + `Promise.allOf`,
  20s timeout) — book's fire-and-forget claim is false (P1.3); honesty fix tracked runtime-side
  by #386 (durable-pubsub D5).
- **Interceptors: seven now** (+`IdempotencyMethodInterceptor`, #398); composition order is
  deterministic — **first annotation = outermost** (`FactoryClassGenerator.java:630`); book's
  example nests opposite to its own prose (P1.4).
- **Durable-entity spec is at v0.3.0** (`aether/docs/specs/durable-entity-primitive-spec.md`
  — note the PATH MOVED from docs/internal/). Signals are v1 (§6.6, "book requirement");
  per-call `ReadConsistency` (§8.1) is specced AND shipped
  (`DurableEntity.java:86-88`, BOUNDED_STALE default | LINEARIZABLE no-op-round). Entity core +
  fencing + `PerKeySerialExecutor` implemented with tests; **workflow/saga/timers still
  spec-only** (epic #345 incr. 5-6; `StateMachineDefinition` relocated to
  `integrations/statemachine`). Module D's error-surface names are **blocked on #432**
  (spec §5.3 `EntityCause` vs shipped `DurableEntityError` — design stream must pick).
- Stream default **RF=1** (`StreamConfig.DEFAULT_REPLICAS=1`): owner failover can read empty;
  the "must not be lost" chapter must disclose (P2.9).
- Modules A (7/7 claims) and the rest of B/C surfaces verified CLEAN — full list in the
  probe file's "Verified clean" section.

## Aether-side context

- **Contract amendment (owner-ruled 2026-07-10, mailbox 09:20):** additive evolution of
  INTERNAL wire-protocol types no longer hard-stops aether-main's merge loop (obligations:
  rebuild-together, compat release-note, README declares protocol types internal).
  Client-facing API + stored formats keep the hard stop. Compensating ticket **#434**
  (protocol versioning / rolling upgrade, rc3, GA-scope) — future anchor for Part V 5.3 and
  Module F; until it ships, ops chapters must say mixed-version clusters are unsupported.
- Drain velocity is real: rc2 went 6 open → 1 open in 4 days; #382 closed same-week on a
  probe comment. The loop premise (probe cheap, prose rots) confirmed by its own first tick.

## Next work, by gate state

- **Pause holds →** probe tick 2 leads (bottom of `PROBE-2026-07-09.md`): `rate-guard`
  reactive category (undocumented; relation to #144?), #339/#198 example projects, Module D
  ch.3/4 re-probe when #345 incr. 5-6 land, #434 tracking.
- **Pause lifts →** execute `PROBE-2026-07-09.md` top-down: P1 items 1-5 (Aspect rewrite,
  error-mapping redesign, publish semantics, interceptor example, signals blockquote), then
  P2, then P3. Item 8 stays blocked on #432. After that: Modules E/F + Parts IV-VI are
  still scaffolds; title + cover still open; versioning coordination with `pfd-editor` open.
- **Either way:** `ai-tools/skills/aether-coder/*` likely shares several stale facts
  (Aspect seam, interceptor count/order, @Heartbeat conflation — see P3.16) — worth its own
  probe pass; skill is in-lane and NOT prose-gated.

## Git state

- **coding-technology** — branch was `site-redesign` (other sessions moved it; was
  `jbct-4.0-self-contained` at the 07-08 handover — don't rely on the branch, my lane is
  untracked anyway). Mine, uncommitted: `book-aether/` + `book-aether-meta/` (both fully
  untracked). `ai-tools/skills/aether-coder/*` modified by an earlier session — untouched
  again this session. Commit = explicit ask only; single-line conventional, no trailers,
  never push unasked, never commit PDFs/EPUBs.
- **pragmatica** — branch `release-1.0.0-rc2`. This session committed NOTHING there (probe
  was read-only). My GitHub artifacts: issues #432/#433, comments on #382/#363, the
  `aether-gap` label, mailbox PROBE TICK 1 entry (mailbox is transport — never committed).

## Working style that paid off (keep)

- **Probe pattern:** 5 parallel read-only agents (one per module/part), classification rubric
  RUNTIME-GAP / BOOK-STALE / TRACKED / UNVERIFIABLE, hard guardrails in every prompt (NEVER
  `mvn` — `mvn verify` with HCLOUD_TOKEN fires paid Hetzner tests). Agents sometimes go idle
  WITHOUT delivering — ping them via SendMessage to get the report; don't re-spawn.
- **Verify before filing:** every GitHub-bound finding re-checked with my own greps; two
  candidate gaps dissolved on verification (interceptor reconfig = design non-goal;
  error-glob = settled by #385). The contract's "many gaps are shipped-but-unreconciled"
  warning proved true in both directions.
- Fidelity first: read the spec fresh each session (path moved once already); never trust a
  handover's spec summary — including this one.
- Memory `project_aether_gap_drain.md` is current (tick 1 + amendment recorded).
