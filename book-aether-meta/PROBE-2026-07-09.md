# Probe results — 2026-07-09 (book-as-QA loop, tick 1)

First run of the probe program. Scope: entire drafted surface (Parts I, II, Modules A–D)
verified against `pragmatica` @ `release-1.0.0-rc2` HEAD by 5 parallel read-only agents;
every finding below spot-checked or evidence-cited. Runtime-side outcomes: filed **#432**
(spec §5.3 vs `DurableEntityError` drift) and **#433** (QueryAnnotationProcessor javadoc),
commented on **#382** (fix verified, close candidate) and **#363** (processor half landed;
`pg-notification` not `pg-notify`). Mailbox entry 2026-07-09 23:26.

**Everything below is BOOK-SIDE and GATED** (prose pause of 2026-07-04). Execute top-down
when the gate lifts. Ordering = blast radius.

---

## P1 — teaches something false or broken (fix first)

1. **Part I: the Aspect factory seam is deleted code.** `part1-no-magic.md:43,49,98-106`
   shows `orderQuote(Aspect<OrderQuote>, SliceCreationContext)` and calls the 2-arg shape
   "the only contract the runtime knows". Runtime deleted the seam (`fd1649ad0`, closed
   #277): generated factory is single-arg `(SliceCreationContext)`; `SliceFactory.java:127-136`
   *rejects* 2-arg factories ("factory parameter 0 Aspect was removed"). Rewrite the
   flagship anatomy example + every mention of Aspect in the bridge story. Real reference
   artifact: `examples/ecommerce/inventory/.../InventoryServiceFactory.java`.
2. **Part I/II: error-glob example can never match.** Globs match the **Cause type's simple
   name only** (`ErrorTypeDiscovery.java:157` → type-pattern switch), never messages, never
   enum constants. Book's `HTTP_402 = ["*PAYMENT_DECLINED*"]` (`part1-no-magic.md:264`)
   matches nothing — the discovered type is `General`. All constants of one enum share one
   status; per-case statuses need record-per-case Causes. #385 (CLOSED) even ships a
   compile-time check (unmapped Cause = build failure, zero-match pattern = warn) — teach it.
   Redesign the Part II `OrderService` error enum + the routing chapter's mapping section.
3. **Module B: `publish()` is not fire-and-forget.** `part3-playbook.md:520` claims publish
   doesn't wait for subscribers; `TopicPublisher.java:37-41` request-response-invokes every
   subscriber and `Promise.allOf`s them (20s default timeout). Book's own sample (line 458)
   blocks the HTTP response on a dashboard DB write. Rewrite semantics + sample. Direction
   of travel: #386 / durable-pubsub-spec D5 ("ephemeral honesty", bare `Unit` return).
4. **Module C: interceptor composition order is fixed and the book's example is inverted.**
   `FactoryClassGenerator.java:630`: first annotation = outermost. Book says ordering is
   "still being settled" (`part3-playbook.md:983-984`) and its example
   `@RetryPayment @BreakOnPaymentOutage @LogCalls` (915-918) produces the OPPOSITE nesting
   of what the prose at 980-982 recommends. Fix both.
5. **Module D: S1 signals blockquote is factually wrong.** `part3-playbook.md:1474-1477`
   says signal injection "deferred to a later version"; spec §6.6/§14-S1 RESOLVED
   2026-07-04: signals ARE v1 (REST `POST /api/workflows/{type}/{id}/signal`, CLI), citing
   "(book requirement)". Rewrite D.5 ops paragraph; add §6.6-based passage to the workflow
   chapter. Saga `WAIT_SIGNAL` stays v2.

## P2 — materially stale/incomplete (fix in the same pass)

6. **Module D: teach per-call `ReadConsistency`.** Zero mentions in the book; spec §8.1 +
   shipped code (`DurableEntity.java:86-88`, `EntityLinearizableReadTest`) have
   `get(K, ReadConsistency)` with BOUNDED_STALE default | LINEARIZABLE (no-op round).
   D.2 interface listing (1142-1152) + reads paragraph.
7. **Module D: banner overclaims the gap** (`part3-playbook.md:1002-1004`): per-key
   serialization HAS landed (`PerKeySerialExecutor`, wired into all three entity impls).
   Still missing: persistent backing, stream-path fence, durable timers (`TimerNotSupported`
   thrown). Split the banner per chapter: entity core = landed+tested; workflow/saga = spec-only.
8. **Module D: error surface names** (1207-1214) copy spec §5.3, which diverged from shipped
   `DurableEntityError` — BLOCKED on #432's resolution; follow whichever surface wins.
9. **Module B: "Events that must not be lost" must disclose default RF=1.**
   `StreamConfig.DEFAULT_REPLICAS=1`; owner failover can read empty (guarantees.md rows
   26/47/52). Chapter (528-626) + TOML sample (611-618) need `replicas`/`minSyncReplicas`
   and the honesty paragraph. Real durability = `replicas>=2 AND minSyncReplicas==replicas`.
10. **Module C: re-scope the #277 flag** (887-890). Settled design: user interceptors are
    compile-time BY DESIGN (RFC-0008; runtime-observability-aspects-spec.md:89 non-goal);
    what IS runtime-switchable: system observability via `/api/aspects` (LOG/METRICS,
    ObservabilityStrategyCells). Rewrite the flag as settled behavior, teach `/api/aspects`.
11. **Module D: S2/S4 blockquotes** (1183-1185, 1324-1328) present settled decisions as open
    (7d/30d retention; C hidden = `Unit`, both RESOLVED 2026-07-04 as the book proposed).
    Convert to settled prose.

## P3 — precision/polish

12. **Module C: seven interceptors, not six** (945): `IdempotencyMethodInterceptor` shipped
    via #398 (2026-07-05). Add.
13. **Module C: manifest category is `pg-notification`** not `pg-notify` (book 866;
    `MethodModel.java:480`). Also update the inline flag at 815-818: processor generation
    HAS landed (728b4deee); only runtime wiring is missing (#363 still open).
14. **Module D: RUN_ONCE precision** (1429-1439): adopt spec language — at-most-once
    *invocation*; effectively-once requires downstream dedup on `(sagaId, stepIndex)`.
    (Agreed with Sergiy pre-pause, never applied.)
15. **Part I: manifest key** `publish.topics.*` → count is `publish.topics.count` but
    per-entry keys are `publish.topic.<N>.*` singular (`ManifestGenerator.java:167-176`), line 181.
16. **Part I/II: `@Heartbeat` is a user-authored meta-annotation**, not framework-shipped
    (each blueprint defines it via `@ResourceQualifier(type=Scheduled.class, ...)`). Book
    presents it as core (196-197). One clarifying sentence; cross-ref the custom-qualifiers
    pattern. Check `ai-tools/skills/aether-coder/` for the same conflation.
17. **Part I: secrets wording** (233): `${secrets:...}` resolves at CLI deploy-time
    composition, not live node lookup; replacement nodes copy the leader's resolved literal
    (#336 workaround). Loosen "resolved at load from the platform's secret store".
18. **Module A: "strict resolution" wording** (229-230): unmatched schema subdir fails via
    generic config validation, not a dedicated pre-flight check. Behavior matches intent
    (loud failure); soften the mechanism claim.

## Verified clean (no action — confidence for the gate lift)

- Module A: 7/7 claims (transport priority incl. exact priorities 20/10/0, pool_config,
  @PgSql codegen + [PG-VALIDATE], migrations V/U/B + `aether_schema_history`, jOOQ,
  custom qualifiers, SqlConnector/RowAccessor surfaces).
- Module B: topic derivation from blueprint coords, pub-sub at-most-once semantics,
  stream at-least-once + offset story, wiring shapes.
- Module C: HttpClient/Notify/SMTP/CB/cache config surfaces.
- Part I: config merge SLICE>NODE>GLOBAL, manifest keys (except #15), routes.toml basics.
- Part II: Ember "always remote via localhost" claim confirmed in `EmberCluster.java`.
- Module D ch.1 (manual saga, plain JBCT) untouched by drift; ch.3-5 banner accurate re
  workflow/saga (unimplemented, epic #345; `StateMachineDefinition` relocated to
  `integrations/statemachine`).

## Follow-up leads (next probe tick)

- `RATE_GUARD_TYPE -> "rate-guard"` reactive category (MethodModel) — undocumented
  everywhere; relation to #144 RateGuard? Investigate, maybe fold into #283.
- #339/#198 example projects still pending; routing chapter re-verify when they land
  (BOOK-PLAN already tracks).
- Module D ch.3/4 re-probe when #345 increments 5-6 (workflow/saga) start landing.
- **#434** (filed 2026-07-10, rc3, GA-scope): protocol versioning / rolling-upgrade design
  is now first-class. Anchor for Part V 5.3 (rolling updates/canary) + Module F
  (versioned-message evolution). Companion ruling (mailbox 2026-07-10 09:20): internal
  wire-protocol types evolve additively under rebuild-together — mixed-version clusters
  unsupported in rc; the book's ops chapters must not imply otherwise until #434 ships.
