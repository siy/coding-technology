# Handover — 2026-07-08 (session "The Editor")

## TL;DR — where things stand

- **The architecture book is real and ~45% raw material.** Name settled (*Architecture Synthesis: The Next Correct Step*), process core designed AND validated (8/8 derivations incl. 3 external blind), question-set audited (11→9), BOOK-PLAN drafted with 13-chapter spine, prep green-lit. Prose: 0% by design. Everything lives in `book-arch-meta/` (uncommitted).
- **Two articles shipped/staged:** "How Architecture Emerges" LIVE (dev.to + Medium + LinkedIn, strong day-1 signal); "Three Famous Architectures, Derived Blind" publication-ready, awaiting user's cover image + go.
- **Aether book delayed** (user decision); **Editor↔aether-main gap-drain loop running** in the pragmatica repo — rc2 milestone at 2 open tickets, nearly cut.
- **Session role:** "The Editor" — book/probe side of the Aether split + architecture-book owner + article engagement support.

## ⚠ Session-start actions for the next session

1. **Re-arm the mailbox watch** (Monitor dies with session): poll mtime of `~/IdeaProjects/pragmatica/aether/docs/internal/coordination/MAILBOX.md`; read anything new first (protocol: silence = no exceptions). Last entry: Editor DECISIONS 2026-07-04 23:46.
2. **Check the Aether drain state:** `gh issue list -R pragmaticalabs/pragmatica --milestone "v1.0.0-rc2" --state open` — was #345 (durable-entity epic, active, increments landing) + #403 (release-gated, closes at rc2 cut). PR #359 awaiting author rebase since Jul 4. If rc2 got cut → decision E (Aether-book sequencing) becomes live.
3. **Ask user about pending manual items** (list below) before assuming state.

## The architecture book — full state

**Files (all in `book-arch-meta/`, never committed; content dir `book-arch/` has root.md only):**

| File | What |
|---|---|
| `BOOK-SCOPE.md` | Name rationale, positioning (procedure vs catalogs/physics; SEI gap), adjacencies (Hohpe, Löwy, Poltorak, Yannick/IVP, SEI), delta-over-module, anti-catalog guardrail, open questions, validation log |
| `BOOK-PLAN.md` | 13-chapter spine in 4 parts, assets→chapters map, **decisions pending A–H** (gate/writing-go, pedagogy/spine order, standalone+PFD overlap, vocab sync, Aether sequencing, reviewers, Aether presence, counterexample intake) |
| `PROCESS-DESIGN.md` | The mechanics: three-spaces model, driver modes (prune/select/split/isolate/bound), next_step v0.1, conflict rule + scope test, 11 process candidates, findings F1–F21, **question-set audit → nine-question sheet v2**, validation scoreboard |
| `LEDGER.md` | Capability/cost ledger v0.1 (6 axes, atomic values, selection rule). **v0.2 queued, not done**: hardware rung+ceiling (F12), containment chain cache→coalescing→replicas→projections (F13/F15), thin tiers (F18), cells (F19), scope exclusion (F20), deadline shapes (F7), boundary cost |
| `DRY-RUN-TICKETING.md`, `DRY-RUN-PAYROLL.md` | Internal validations (5 derivations; refinements: scope test, demand decomposition, null vector, F1–F10) |
| `BLIND-DERIVATION-SO.md`, `-SHOPIFY.md`, `-DISCORD.md`, `-PREDICTIONS.md` | External validations, pre-registered + graded. SO's "read replicas" miss = article-2 anchor ("the method caught the wrong assumption" — user's framing) |
| `WORKED-FAILURE-CASE.md` | Global order book contradiction — mechanical detection (emptied axis + failed scope test + decomposition-stable demands), priced renegotiation menu. Ch. 8 anchor |
| `NOTES-mttr-mtbf.md` | Ch. 12 research done: Allspaw origin post (scope qualifier in title + 2017 self-retraction), VOID/Davidovič metric kill, Google CRE = priced answers, GameDay boundary. Arc sketched |
| `ENGAGEMENT-2026-07-06.md` | Article engagement log, ALL reply drafts, article-2 publication checklist + announce draft |

**Key design decisions on record:** nine questions (three budgets time/failure/loss + consistency + load + external constraints + release structure + cost&capacity + multi-X); questions presented by driver mode; membership criterion (presses/prunes an axis independently); articles stay at eleven (user: "research work deserves the right to mistake/change/adjustment" — the book narrates the audit); axes all pass, watch item: read/write model never moved in blind derivations (fold-into-storage candidate); domain-shape facts stay outside the sheet (Phase-1 output, second row-source).

**Agreed next prep (decision-free):** Ledger v0.2 → worksheet one-pager (doubles as counterexample intake + lead magnet) → metrics baseline (arms decision A). Then: budget arithmetic (ch. 5, emptiest), read Yannick's Zenodo papers (cited-not-read — blocks References + reviewer approach), boring-enterprise blind derivation.

## Articles & engagement

**Article 1 — LIVE:** dev.to https://dev.to/siy/how-architecture-emerges-1b9 · Medium https://medium.com/@sergiy-yevtushenko/how-architecture-emerges-2626fca4fe52 · committed `16091ef`. Medium has cover (crystal image `articles/assets/emerging-architecture.png`) + table fix; **dev.to cover still pending** (~2.4:1 crop, mind the spike). LinkedIn announce + counterexample invitation posted (invitation closes "a method improves on counterexamples, not on applause").

**Article 2 — READY, NOT PUBLISHED:** `articles/architecture-derived-blind-test.md` ("Three Famous Architectures, Derived Blind", `published: false`, no tables, no canonical_url — deliberate, avoids dev.to uniqueness rejection). Full checklist + LinkedIn announce draft in ENGAGEMENT file. Missing: user's cover image (suggested: same crystal language, three structures). Publish flow: flip flag → cross-post → covers per platform → commit AFTER publish (public repo!) → announce with real links → comment on article-1 thread routing readers.

**Engagement state:** Yannick L. (IVP) endorsement repost — ally, potential reviewer, papers unread. Galyen (LinkedIn): 3 rounds, converging; round-3 reply posted; he's unknowingly at the transport-transparency position. Telegram critiques (Max Grom/SEI, Vania Leyn/axes, Andrii Kurdiumov/money) — all mapped to planned content; **four reply drafts in ENGAGEMENT file, posting status unknown — ask user.** Max Grom is owed a pointer when the book ships with nine questions (his critique co-triggered the audit). Interlocutor profiles + strategy: memory `project_pfd_interlocutors.md`.

## Aether side

- Coordination contract in `pragmatica/aether/docs/internal/coordination/MAILBOX.md` (DECISIONS entry = authoritative). Merge grant: green-suite merges pre-authorized, hard stops = API-breaking (6-point mechanical list) + destructive; verification bar = targeted per merge + full sweep periodic.
- aether-main runs the drain autonomously. Editor's unfulfilled side: **zero `aether-gap` issues filed** — the book-probe never ran (session went to the architecture book instead). The label exists, unused.
- Slowdown diagnosis (session start): book-as-QA-program + baseline distortion + long-tail regime. Gap register = the issue queue.

## Pending user-side items (ask, don't assume)

1. Four Telegram/LinkedIn replies — posted?
2. dev.to cover for article 1 — done?
3. Article-2 cover image — created?
4. Metrics baseline (dev.to views, Medium stats, Leanpub sample downloads) — never taken, blocks decision A.
5. Decisions A–H discussion — user deferred; raise when they signal.

## Conventions & gotchas (this session's additions)

- **Never-commit set now includes** `book-arch/` + `book-arch-meta/` (plus the standing set: aether-coder skill files, book-aether*, HANDOVer-*). Commit = explicit ask only; single-line conventional, no attribution; main pushes bypass PR rule via admin (remote warns — expected).
- **Text the user pastes = plain ASCII, no em-dashes** (LinkedIn/Telegram replies, announces). Articles: em-dashes structural-only (lists), near-zero in prose; watch AI tells ("The part nobody tells you", "Here's what matters") — user actively flags them.
- Clipboard workflow: `pbcopy <<'EOF'` + verify via `pbpaste | head -1`; re-copy on request without comment.
- dev.to rejects duplicate canonical_urls account-wide; Medium strips tables (convert to `Axis: values` lines).
- Blind-derivation protocol: research agent gathers two strictly-separated sections (answer sheet / known outcome) with URL+quote citations; predictions pre-registered BEFORE data; every step cites an answer, never the outcome; misses graded and celebrated.
- Article naming: title = argument or label, subtitle carries the rest; user picks final titles (chose "How Architecture Emerges" / "Never Debate Architecture Again" over my options).
- Memory files updated this session: `project_arch_book`, `project_aether_gap_drain`, `project_mttr_mtbf`, `project_pfd_interlocutors` (+3 new interlocutors), `project_quotable_lines` (+8 lines). MEMORY.md index current.
- ndx palace chore backlog (1000 unclassified drawers) flagged at session start — untouched, optional `/ndx-chore`.

## Leanpub / site (unchanged from 2026-07-04 handover)

PFD 2.0.1 live (full + condensed sample); JBCT 4.2.1 live; site deployed through PR #34 + article commit `16091ef`. LinkedIn PFD sample-angle post still queued (pre-dates this session's articles — sequencing may need rethink given the article funnel now exists; surface to user).
