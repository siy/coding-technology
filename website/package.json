{
  "name": "pragmatica-dev-website",
  "version": "1.0.0",
  "description": "Static website for pragmatica.dev",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir dist",
    "dev": "npm run build && python3 -m http.server 8000 -d dist",
    "prebuild": "npm run clean",
    "test": "node --test next-step/*.test.js lib/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
