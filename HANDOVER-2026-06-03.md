# Handover — PFD book, current state (2026-06-03)

Supersedes `HANDOVER-2026-05-28.md` for current state. Durable reference sections of `HANDOVER-PFD-BOOK.md` (methodology framework, voice/tone, narrative threads, commitments) remain valid — read those for orientation; read this for "where we are and what's next." The status section of `HANDOVER-PFD-BOOK.md` is stale (already flagged there).

---

## Where we are

**Drafting complete + two cofounder-review rounds actioned + a full author-review pass done.** Cofounder verdict: **ship-ready** (the three review-round nits were optional polish, all closed). The book reads end-to-end, builds to PDF, and has a cover.

| File (in `book-pfd/`) | Role |
|---|---|
| `introduction.md` | thesis, convergence, five principles, running-example (now two-domain framing), reading guide |
| `acknowledgments.md` | front-matter credits: Jackson, King, Loth (IVP **+ cohesion**), Poltorak |
| `spiral-0-decisions.md` | the decisions a use case forces, before methodology |
| `foundations.md` | vocabulary: shapes, properties, patterns, telescope, recovery triple, **+ "A process gathers knowledge"**, **+ two-axis cohesion (purity/completeness)** |
| `spiral-1-use-case.md` | use-case altitude; **+ ASCII dependency graph** |
| `spiral-2-workflow.md` | workflow altitude; **plain interfaces (no `Workflow.WithPromise`)**, two-check cohesion beat |
| `spiral-3-subsystem.md` | subsystem altitude; refund placement = **purity** call |
| `spiral-4-system.md` | system altitude; system-SLO sentence closes the hourglass |
| `architecture-synthesis.md` | Phase-4/5/6, six axes, recovery, continuous transformation |
| `brownfield.md` | **payroll/HR domain** (not ticketing); audit = **purity/completeness audit** |
| `closing.md` | reflexive taper-proof, scope, falsifiable bet, predictions, invitation |
| `references.md` | back matter: King, Loth (IVP + cohesion), Poltorak, Yevtushenko |
| `root.md` | reading-order index (NOT included in the PDF) |

Word count ≈ **48K** (closing reads "just under fifty thousand words"). PDF ≈ **118 pages** (xelatex/DejaVu + per-chapter breaks). Three ASCII diagrams, one per lower-altitude pass: dependency graph (spiral-1), saga/compensation (spiral-2), subsystem boundaries (spiral-3).

`book-pfd/` is **manuscript-only**. Scaffolding lives in `book-pfd-meta/` (see below).

---

## Latest round — cofounder feedback actioned (2026-06-03)

Cofounder reviewed the revision: verdict **ship-ready**; called "A process gathers knowledge" "the most important thing added," and two-axis cohesion "strategically most valuable." Acted on his three "decide" items plus a pre-send accuracy pass:

- **Accuracy pass on every Loth reference** (he is the one reader who will scrutinize his own work). Fixed titles in `references.md`: Dev.to article = *The Independent Variation Principle as a Thought Framework* (2025); formal paper = *The Independent Variation Principle (IVP)* (2025); cohesion subtitle lowercased to *Cohesion as a two-axis schema*. Both DOIs verified to resolve (IVP `10.5281/zenodo.17677316`; cohesion `10.5281/zenodo.20492913`; both CC BY 4.0). Independence framing ("corroboration, not foundation") confirmed intact everywhere.
- **Knowledge-gathering threaded** beyond Foundations: a nod in the Introduction's thesis and a parting line in the Closing. Deliberately **not** a new numbered thread — it recurs without a tag.
- **Two cohesion axes owned as vocabulary** (purity, completeness): added to the Foundations summary list ("…three recovery classes, **two axes of cohesion**, one organizing structure") and bolded as terms. Rationale: a foundational pair introduced once and reused at every altitude passes the same test the six patterns pass — consistent with "the vocabulary doesn't grow *per altitude*." (Author's call, overriding the agent's initial hedge to keep them out of the list.)
- **Two more diagrams** added: saga/compensation (spiral-2: forward chain ↔ inverses, unwind in reverse) and subsystem boundaries (spiral-3: three boxes, thin versioned contracts). ASCII, kept whole on one page by the build's minipage wrap.

**Foreword (decided):** the author will ask Loth to read the book and write a **foreword** — keep his work independent (higher value than co-authoring), credit prominently. Yannick is now an **active collaborator** (IVP + cohesion both load-bearing); protect the independence framing as the collaboration deepens. The book is ready to receive a foreword: add `foreword.md`, wire it into `root.md` + the `build-pdf.sh` CHAPTERS list, and it renders ahead of the Introduction.

---

---

## This session's work

### Two cofounder-review rounds (both fully actioned)
- **Round 1 (repetition + defects + single-domain):** cut duplicated prose (spiral-2 theory dedup, spiral-3 six-patterns → table, spiral-4 observability 3×→1, synthesis audit-trap 3×→1, brownfield triple-ending → one, Metapatterns note de-duplicated); fixed code/naming defects (**`CompleteBooking.Request` raw `String`/`int` → typed value objects** restoring parse-don't-validate; `Promise.all` vs `allOf` clarified; dangling `UseCase.WithPromise` reference reworded; hold-step name unified to `HoldOrExtend`); added the **system-SLO** sentence; glossed **BER/FER/design-out** before the axis table.
- **Round 2 polish:** spiral-2 growing-context near-duplicate collapsed; brownfield anemic example re-centered on **`Payslip`** (so failure modes don't all cluster on `Employee`); a ~150-word run-on split; the dense Metapatterns blockquote lightened.

### Brownfield re-domained to **payroll/HR**
The single most legitimate critique ("every example is ticketing") got the strong answer: Brownfield was fully rewritten onto a payroll/HR system — clock-driven, not request-driven; legal payslips, filing windows, pre-tax-deduction ordering. The intro now frames this as deliberate ("the other half of the test": one domain shows magnification, a second shows the method travels). Spiral + Architecture Synthesis stay ticketing.

### Author-review pass (this session, applied after the user read the draft)
- **Intro:** running-example claim scoped to the spiral+synthesis arc; new paragraph framing brownfield's payroll as the portability test.
- **Foundations:** (a) telescope now tops out at *system* with the enterprise rung named and *why* it's excluded (organizational/strategic, Conway's law, not change-driver cohesion the code expresses); (b) new **"A process gathers knowledge"** section (failure is knowledge; the process ends when it knows enough to answer; "what does this process need to know?" → per-process types); (c) Jackson's "semantic potential" landed in the body (was only in Acknowledgments).
- **Spiral 1:** ASCII **dependency graph** added to "Dependencies between steps."
- **Spiral 2:** dropped `extends Workflow.WithPromise` from both code blocks → plain interfaces with explicit `Promise<Response> apply(Request request)`; the type's value moved to prose. (It was unearned ceremony, hidden the contract, and was inconsistent with Pass 1, which shows no base-type pattern.)

### Loth two-axis cohesion — the biggest content addition
New Loth paper: ***On the Nature of Cohesion*** (Zenodo, 2026-06-01, DOI 10.5281/zenodo.20492913, CC BY 4.0). Cohesion = "correctness relative to a modularization principle," two independent axes: **purity** (no element the principle separates) and **completeness** (all elements it unites). Failure modes: purity-without-completeness = *shotgun surgery*; completeness-without-purity = *accidental coupling*. His LCOM critique (structure mistaken for cohesion) independently corroborates PFD's "shared-data view has the arrow backwards."

Threaded as **practical diagnostics** (not metrics theory):
- **Foundations** — the recognition test now has its two named directions + a credit paragraph (PFD's change driver *is* such a principle; structural metrics miss cohesion because they measure what code shares, not what governs it).
- **Spiral 2** — booking workflow shown passing both checks.
- **Spiral 3** — refund placement named a **purity** call.
- **Brownfield** — "boundaries that predate reasons" audit reframed as a purity-and-completeness audit.
- **References + Acknowledgments** — added as Loth's second work.

**Fidelity choice:** instantiated Loth's schema with PFD's change-driver as the principle; **deliberately omitted his SRP polemic** (Theme 25 positive-register guardrail) — credit the convergence, stay positive. Goal: Loth recognizes his framing doing real methodology work.

### Cover + PDF build pipeline (new this session)
- **Cover:** `book-pfd-meta/cover.svg` — a single reproducible SVG in JBCT's idiom (black geometric line-art on warm paper) with a **telescoped-hourglass** hero (four nested levels → the system-altitude neck → reopening) and **one amber node at the neck** (the lone spot of colour, a quiet brand nod). Pure B/W body for JBCT series cohesion; user explicitly chose B/W + amber-neck over an earlier all-navy version. `cover-preview.png` is the committed render.
- **Build:** `book-pfd-meta/build-pdf.sh` — `pandoc → xelatex`, **DejaVu Serif/Sans/Mono to match the JBCT book**, per-chapter page breaks, code blocks kept whole (etoolbox minipage wrap), DRAFT watermark + draft date in draft mode, cover prepended via `pdfpages`. Verified V₀/→/↔ render natively in DejaVu (no glyph hack needed). `./build-pdf.sh` = draft; `./build-pdf.sh --final` = no watermark, output `process-first-design.pdf`. Generated PDFs are **gitignored** (`book-pfd-meta/*.pdf`).

---

## Durable decisions captured this session

1. **Brownfield = payroll/HR**, a deliberate second domain (portability test). Spiral + Synthesis remain ticketing. Intro frames the two-domain split.
2. **`Workflow.WithPromise` dropped from the code** — plain interfaces + explicit `apply`; the uniform-invocation value stated in prose. Consistent with Pass 1.
3. **Loth two-axis cohesion threaded as practical diagnostics**, his SRP polemic omitted. Commitment to show Loth the finished draft is now doubly relevant (his cohesion paper is integrated).
4. **Cover** = `book-pfd-meta/cover.svg`, B/W telescoped hourglass + amber neck node; JBCT idiom.
5. **Build** = xelatex + DejaVu (match JBCT); chapter breaks; code-blocks-whole; watermark in draft; PDF gitignored.
6. **Manuscript/scaffolding split**: `book-pfd/` ships; `book-pfd-meta/` holds scaffolding + build assets.

---

## Build / regenerate

```
cd book-pfd-meta
./build-pdf.sh            # draft: DRAFT watermark + "Draft — <date>" → process-first-design-DRAFT.pdf
./build-pdf.sh --final    # final:  no watermark, "<Month Year>"       → process-first-design.pdf
```
Requires `pandoc`, `xelatex`, DejaVu fonts (installed), and `rsvg-convert` for the cover (falls back to `cover-preview.png`). Output is gitignored.

---

## Open items / next actions

- **Ship build:** run `./build-pdf.sh --final` when ready (drops watermark/draft date).
- **Font size:** body is 11pt DejaVu; the JBCT book is 10pt DejaVu. Font family now matches; match 10pt too only if a tighter visual match is wanted (one flag).
- **Loth:** send the finished draft to Yannick Loth pre-publication (now integrating two of his works — IVP + cohesion).
- **Glossary** (`../oss/content/pfd-example-glossary.md`): Architecture Synthesis vocab swept in; brownfield (payroll) vocab is kept **local to the chapter** by design, not in the ticketing glossary.
- **`HANDOVER-PFD-BOOK.md`** status section remains stale (banner already added); durable sections valid.
- Optional: wire `cover.svg → manuscript/images/title_page.png` if moving to a Leanpub-style manuscript build; render the cover with Inter installed for the final.

---

## Canonical sources (read before new drafting)

- `../oss/content/pfd-book-spec.md` — structure, code/citation conventions, methodology framework, Acknowledgments tracking.
- `../oss/content/pfd-book-voice.md` — prose discipline (§5 spiral-pass, §6 AI-tells incl. em-dash ≤2, §9 checklist, forbidden vocab).
- `../oss/content/pfd-book-considerations.md` — Themes; esp. **Theme 25** (change-driver cohesion = NOT shared data; Loth/IVP convergence; **presentation traps incl. over-formalism + no-SRP-teardown** — the guardrail used for the Loth cohesion integration), Theme 26 (telescope/hourglass), Theme 23 (CodersWorld brownfield bar).
- `book-pfd-meta/REVIEW-spirals-2026-05-27.md` — earlier co-founder review (Passes 1–3), all actioned.
- `book-pfd/root.md` — reading order.

---

## Conventions in force (apply to all new drafting)

- **Vignette** (italic) at chapter top; **threads-advanced** tag at foot.
- **Em-dash ≤2 per prose paragraph** (definition lists and table cells exempt). Verified clean across all edits.
- **Forbidden vocab** in publishable body: "non-functional"/"NFR"/"system-level requirement"/"system-input requirement" — except the single deliberate naming-to-refuse at `architecture-synthesis.md` (the audit). Use "quality requirements"/"system qualities"; at detail, SLO/constraint/operational target/substrate-shaping.
- **Product names:** none, **except** Aether (where the slice deployment mode is load-bearing) and positive crediting (Jackson, King, Loth, Yevtushenko, Poltorak).
- **Register:** research-cited, sharp, positive — **no competitor/SOLID teardown** (this governed how Loth's SRP critique was *not* imported).
- **Code:** pseudo-code for type shapes, Java for composition; JBCT idioms strict (single-param `Result<T>`, `Promise<T>` carries failure, `Unit` not `Void`, `TypeName.typeName(...)` factories, method references, sealed `Cause`). Workflows are plain interfaces with an explicit `apply`, not a base type.
- **Cohesion vocabulary (new):** the change-driver recognition test has two directions — **completeness** (all the driver governs; miss → shotgun surgery) and **purity** (only what it governs; mix → accidental coupling). Both required.
