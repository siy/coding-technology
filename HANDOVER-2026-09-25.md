# Handover — 2026-09-25 (session: pfd-editor)

Continues `HANDOVER-2026-09-19.md`, which stays here as the immediate predecessor; `09-17` moved to the
private record with this one. **The git history of this repository was rewritten on 2026-09-23.**
`main` is `d898d3b` (#89). Working tree clean, all gates green. Open PRs: #83 only.

Versions unchanged and unpublished: JBCT **5.0.0**, PFD **3.0.0**, AS **1.1.2**, Aether 0.1.0 draft. No tags moved
except by the rewrite (same names, new SHAs).

---

## Read this first: four things that will bite a successor

**1. EVERY COMMIT SHA FROM BEFORE 2026-09-23 IS DEAD.** Owner-ordered purge: `git filter-repo` removed the
third-party engagement log, a reviewer's private feedback, the Aether voice overlay and 33 archived handover
paths from every commit, and rewrote the owner's home-directory paths and every path into the private
repositories. 670 commits became 643; all 58 tags were rewritten; GitHub's signature badges on old merges are
gone. **Any SHA cited in an older handover, PR body, ticket or private record resolves to nothing here.** The
old-to-new map lives in the private record (committed there as `0503c4f`; its path is in project memory).
Translate through it; never conclude a cited commit "never existed".

**2. THE PURGE IS NOT COMPLETE, AND THAT IS AN OWNER DECISION, NOT AN OVERSIGHT.** GitHub still serves old
commits by SHA — verified after the push: an old commit resolved through the API — via 82 read-only
`refs/pull/*` refs and cached views. Only GitHub Support can clear those; the owner ruled *leave it as is for
now* (2026-09-23). One fork created 2026-06-22 holds an older copy of the reviewer feedback; the owner ruled
to keep it as is. A drafted Support request and an untouched backup of the pre-purge history sat in that
session's scratchpad, which is temporary — **assume both are gone.** If the owner revisits, regenerate the
request from the CHANGELOG entry.

**3. A PUSH THAT MOVES MANY TAGS FIRES NO WORKFLOW.** The force-push updated 3 branches and 58 tags at once
and GitHub created no `push` event for `main`, so `deploy.yml` did not run. Harmless here (no site file
changed, and #88/#89's merges deployed normally), but a green-looking "pushed" does not mean "deployed".
Read the run list.

**4. `oss` sweeps `internal/` only. Anything dropped elsewhere there — `content/` especially — must be
named to the `oss` session directly.** The owner authorized talking to that session directly (2026-09-23).
Drop, message it with the exact paths, then verify its commit **by content** (`git rev-parse <sha>:<path>`
against `git hash-object`) — it reports what it committed, and that report is a claim.

---

## What changed since the last handover

| | |
|---|---|
| **History purge** | Force-pushed 2026-09-23 after an independent adversarial verification: 0 residual username/private-path hits in every blob, message and tag (vs hundreds in the control), every tree difference accounted for across all 61 refs, all five gates green on the rewritten `main`. |
| **#88** | Merged. The 09-19 handover. |
| **#89** | Merged. The Aether voice overlay's four references now name it as private; CHANGELOG records the purge. |
| **Branches** | `release-2.0.12` (superseded by v2.1.0) and `fix/deployed-site-headers` (merged as #78) deleted with `-D` on owner ruling; four merged leftovers deleted. Only `book-aether/resume-2026-09-02` (#83) remains. |
| **Audits** | The Discussions category exists (open-ended, created 2026-09-24 00:05 UTC); `count-audits.sh` resolves it and reports 0. **Not linked from the site until R1/R2 are green** (pragmatica #872). |

**Owner rulings 2026-09-23:** all voice documents are private; `article-cross-poster` stays untracked (internal
tool); the headers fix was verified live (security headers on `/` and `/style.css`, legacy redirects 301).

**#86's privacy audit had missed six live username paths** (`FRESH-READ-LEARNER.md`, the synthesis-spec
handover): it searched for private-*repository* paths, not the home directory in general. The purge fixed them.
A privacy sweep needs the username as its own pattern.

---

## The article: "One Graph, Two Notations" (draft, unshipped)

Thesis: with delivery abstracted out, a JBCT call chain and an event-driven flow are one graph in two
notations. Lineage: Lauer & Needham, "On the Duality of Operating System Structures" (OSR 13(2), 1979) —
borrowed as a *method*, never as a theorem covering EDA. Evidence: the 65-pattern EIP catalog classified by two
blind raters, 64/65 agreement; 43-44 delivery, and the remaining 21-22 land on JBCT's catalog with two named
exceptions (Routing Slip, Canonical Data Model).

The draft and its evidence live in the private record (drafts stay private until shipping). An adversarial
fact-check found twelve defects in draft 1, all fixed in draft 2 — the worst was that **the count's own
instrument filed Publish-Subscribe under delivery, removing the very break the article argues**; the article now
says so and takes the break from the paper's table instead.

**Owner decisions pending before it can ship:** length (2,085 words vs the 1,500-1,800 benchmark); the title
(my coinage); disclosing the raters were AI agents; publishing the rater tables beside it. **Ping the `oss`
session after every revision** — it commits drafts only when told.

---

## Open

**Needs the owner:**
- **The four article decisions** above.
- **GitHub Support** for the cached history — deferred by ruling; not done, stated as not done.
- **The em-dash question** — *"JBCT prose uses spaced hyphens, PFD/AS use em-dashes"* — still unruled and unwritten.

**In this repo:**
- **#83** — open, for review not merge; stamped at rc3 while rc4 is upstream.
- **#61** blocked on pragmatica #872/#873. **#16** open.

---

## Working notes

- **Search patterns lie in both directions.** A word search for `actor` matched every `factory`; a live-set
  computed from *all* branches included #83's pre-archive tree and nearly exempted 28 handovers from the purge.
  Both were caught only because the number looked wrong.
- **A bucket defined by exclusion makes half its result true by construction.** "Delivery = would not exist
  in-process" guarantees the remainder survives in-process; only *where it lands* is a finding. Say which half
  is which in the published text.
- **Two blind raters beat one rater plus a checker** for a judgement call: their disagreements are the soft
  spots, and here they converged on the same five uncertain patterns without seeing each other's work.
- **Preserve before you destroy, by hash.** Four handovers superseded before the archive rule existed had no
  private copy; the purge would have destroyed the only ones. Found by hashing every archived path against the
  private tree, not by trusting "the archive is preserved".
